# 개선사항 체크리스트

우선순위/순서대로 하나씩 처리. 완료되면 [x]로 표시하고 무엇을 어떻게 고쳤는지 한 줄 남길 것.

## 요약

| 번호 | 상태 | 한 줄 요약 |
|---|---|---|
| 1 | ✅ | Key-gen 에러 — 호스트 키(SSH 지문) 변경 감지 후 사용자 승인 흐름 추가 |
| 2 | ✅ | 터미널 복사/붙여넣기 안 됨 — xterm.js paste 리스너 중복 버그 수정 |
| 3 | ✅ | 동적 IP 원격지 자동 재탐색 — MAC 기반 새 IP 탐색, SSH+마운트 양쪽 적용 |
| 4 | ✅ | 시트 프로파일도 MAC 미리 기록 — 5번의 범위 확장 작업에서 해결됨 |
| 5 | ✅ | MAC 실패 시 mDNS 2차 보험 — 구현 완료, 도커 대역 필터링까지 반영 |
| 6 | ✅ | 화면에 MAC/재탐색 상태 안내 — 상태줄/배너/원격지별 배지/수정 모달로 전부 해결 |
| 7 | ✅ | 저장된 IP 겹침 위험 — 8번의 MAC 기반 신원 검증으로 해소 |
| 8 | ✅ | 7번 최종 대응 5가지 개선 — mDNS 다중후보/정보수정/MAC 불일치/시트 불일치/중복 IP |
| 9 | ✅ | 배포 후 WebView2 캐시 문제 — 버전 마커 파일로 해결 |
| 10 | ✅ | 라우팅/터널 경유 MAC 사각지대 — SSH로 직접 MAC 조회하는 방식 추가 |
| 11 | ✅ | nmap 기반으로 탐색 방식 전면 전환 |
| 12 | ⬜ | Ward 전환 시 원격지 개수만큼 SSH 접속이 동시에 몰림 — 지연 로딩 등 개선안 검토 |

## 1. Key-gen 에러
- [x] 완료 — 정체 확인됨: 서버 IP가 재할당(동적 IP 등)되면 그 자리에 다른 장비가
  들어와 SSH 지문(host key)이 이전과 달라지고, paramiko가 `BadHostKeyException`을
  던지며 접속을 거부함. 표준 SSH 클라이언트에서는 이럴 때 `ssh-keygen -R <호스트>`로
  `~/.ssh/known_hosts`에서 그 항목을 지우고 재접속해서 새 지문을 등록하는 게 흔한
  절차라 "keygen 에러"라고 불린 것으로 보임. 다만 우리 앱은 표준 known_hosts가 아니라
  자체 `known_hosts_launcher` 파일을 쓰기 때문에 그 명령이 우리 파일엔 영향을 못 줌.

  → `ssh_session.py`에 `_handle_changed_host_key()` 추가: 지문이 바뀐 걸 감지하면
  이전/새 지문을 보여주고 사용자 승인을 받은 뒤(중간자 공격 가능성도 있어 자동 덮어쓰기는
  안 함), 승인 시 우리 known_hosts_launcher에서 해당 항목을 지우고 새 지문으로 갱신한
  뒤 자동으로 재접속. "처음 보는 서버" 승인 흐름(`InteractiveHostKeyPolicy`)과 동일한
  방식 — 사용자 경험 통일. 실사용 검증 필요.


## 2. 터미널 복사/붙여넣기 안 되고 이상한 문구 발생
- [x] 완료(1차) — 원인: xterm.js는 내부적으로 자체 textarea에 paste 이벤트 핸들러를
  갖고 있는데, `document`에 추가로 paste 리스너를 붙여서 붙여넣기가 두 번(중복)
  전송되던 게 "이상한 문구"의 정체 → 중복 리스너 제거로 복사는 해결됨(사용자 확인).

- [x] 추가 수정(2차, 원인 오판) — Ctrl+V가 "^V"만 찍히던 문제를 "WebView2가 네이티브
  paste 이벤트를 아예 안 준다"로 잘못 진단하고, tkinter `clipboard_get()`을 파이썬
  쪽(`get_clipboard_text`)에서 읽어와 `term.paste()`로 직접 주입하는 방식으로 바꿈.

- [x] 추가 수정(3차, 진짜 원인) — 2차 수정 후 이번엔 "^V는 안 뜨는데 두 번 붙여넣기
  됨" 증상으로 바뀜. xterm.js 소스(`xterm.min.js`)를 직접 까본 결과: customKeyEventHandler가
  `false`를 리턴하면 xterm은 자기 처리(=^V 변환)만 건너뛸 뿐 `e.preventDefault()`를
  호출하지 않는다는 걸 확인 — 그래서 브라우저 네이티브 paste 동작이 그대로 살아있고,
  그게 xterm 내부 textarea에 떨어지면 xterm이 이미 자체적으로 갖고 있던 paste
  리스너가 알아서 처리해줌. 즉 네이티브 paste는 원래 잘 동작하고 있었고, 2차에서
  덧붙인 수동 클립보드 주입이 그것과 겹쳐서 중복된 것. → Ctrl+V는 `return false`만
  하고 직접 개입하지 않도록 되돌림(네이티브 paste + xterm 내장 처리에 위임).
  tkinter 클립보드 브리지는 우클릭 붙여넣기(네이티브 paste 이벤트가 안 뜨는 경로)용으로만 유지.


## 3. 동적 IP 원격지 — IP 바뀌면 자동으로 재탐색
- [x] 구현 완료, 부분 검증됨 — 1번(key-gen 에러)에서 이어진 요청. 수동 등록한
  (`source="manual"`) 원격지가 동적 IP를 쓰다가 IP가 바뀌면, MAC 주소를 기준으로
  같은 네트워크 대역을 스캔해서 새 IP를 자동으로 찾아 `profiles.json`을 갱신하고
  재접속한다. 시트에서 온 프로파일(`source="sheet"`, 고정 IP + 새로고침 버튼으로
  이미 갱신 가능)은 대상에서 제외.

  - 신규: `mac_discovery.py` — `get_mac_for_ip()`(접속 성공 시 ARP 테이블에서 MAC
    확보), `find_ip_for_mac()`(예전 IP와 같은 /24 대역에 핑 돌려 ARP 캐시 채운 뒤
    MAC 일치하는 IP 탐색, 약 2~3초 소요)

  - `profile_store.py` — `update_profile_field()` 추가(마운트/시트 동기화의 "내용
    같으면 다시 안 씀" 로직과 안 겹치게 분리), `get_profile()`에 `mac`/`source` 포함

  - `ssh_session.py` `connect_profile()` — 접속 실패(`socket.timeout`/`OSError`만,
    비밀번호 오류 등은 안 건드림) 시 매칭되는 조건이면 재탐색 시도 → 성공하면 그
    IP로 재접속. 접속 성공 후 MAC을 아직 모르면 그 김에 캡처해서 저장.
    지문 충돌(1번 기능)과 자연스럽게 연계됨 — 재탐색으로 찾은 새 IP가 예전에 다른
    장비가 쓰던 IP라 지문이 안 맞으면, 이미 있는 `_handle_changed_host_key()` 팝업이
    그대로 뜬다(추가 코드 불필요, `_connect()` 헬퍼로 공용 경로 탐).

  - 실제 네트워크(192.168.111.0/24)에서 end-to-end 테스트 완료: 일부러 틀린 IP로
    등록 → 접속 실패 → MAC으로 올바른 새 IP(192.168.111.11) 자동 탐색 → 프로파일
    갱신 → 새 IP로 재접속 시도(SSH 인증 단계까지 정상 도달)까지 확인. 테스트 중
    실제 버그 하나 발견/수정: 로그 `print()`에 "—"(em dash) 문자를 썼다가 한글
    윈도우 콘솔 기본 인코딩(cp949)에서 `UnicodeEncodeError`로 그 자리에서 죽는
    문제가 있었음 — 일반 하이픈으로 교체.

  - ARP 기반이라 **앱이 실행되는 PC와 대상 장비가 같은 로컬 서브넷에 있을 때만**
    동작함(라우터 넘어가는 `loclx.io` 터널 경유 등은 대상 아님 — 애초에 대상에서
    자동 제외됨).

  - [x] **마운트(드라이브 연결) 경로까지 확장 완료.** 처음엔 SSH 터미널/볼륨 확인
    기능(`ssh_session.py`)에만 붙어있어서, 앱의 핵심 기능인 드라이브 마운트에는
    적용이 안 되는 빈틈이 있었음 — `mount_control.py`의 `mount_profile()`은
    paramiko가 아니라 rclone rc API를 쓰는 별개 경로라 처음엔 재탐색을 안 탔음.
    다만 rclone 쪽 실패는 "IP가 안 닿아서"인지 "비번이 틀려서"인지 구분 안 되는
    뭉뚱그려진 `RuntimeError`로만 나와서, ssh_session.py처럼 예외 타입으로 조건을
    걸 수가 없었음 → rclone을 부르기 전에 `host_resolver.py`의 `is_reachable()`
    (원래 이 파일 안에서만 쓰던 private 함수를 공개로 전환)로 우리가 직접 먼저
    TCP로 닿는지 확인하고, 그 결과로만 재탐색을 걸도록 설계해서 원인을 확실하게
    구분함. 지연시간(재탐색 시 2~3초 추가)은 폴더를 열 때 한 번 하는 동작이라
    문제없다고 판단(사용자 확인). 같은 네트워크(192.168.111.0/24)에서 end-to-end
    테스트 완료: 틀린 IP 등록 → 응답 없음 감지 → MAC으로 올바른 새 IP 자동 탐색 →
    프로파일 갱신 → 새 IP로 마운트 시도까지 확인. 테스트 후 임시 프로파일/rclone
    프로세스/마운트 상태 전부 정리 확인함.

  - [x] **호스트 이름(mDNS/avahi) 기반 방식은 검토 후 채택 안 하기로 결정.** MAC
    대신 호스트 이름으로 IP를 찾는 방법도 검토했으나, 테스트 서버·실제 병원 서버
    둘 다 `avahi-daemon`이 설치는 되어 있어도 실행 중이 아님을 확인(`ps`로 확인).
    평소에 호스트 이름 조회 자체가 안 되는 환경이라 판단, MAC+ARP 기반 방식을
    그대로 유지하기로 함 (2026-08-03).


## 4. 시트에서 온 프로파일(`source="sheet"`, 고정 IP)도 MAC 주소를 미리 기록해두기
- [x] **5번의 "범위 확장 완료" 작업에서 해결됨** — 당시엔 "기록만 할지, 재탐색 로직까지
  적용할지" 결정이 안 나 있었으나, 5번(2026-08-05)에서 `source == "manual"` 제한을
  캡처/재탐색 양쪽 모두에서 제거하기로 하면서 두 질문 다 해소됨. 현재 코드
  (`ssh_session.py`/`mount_control.py`)에 `source` 조건이 전혀 없음을 확인
  — 시트 프로파일도 캡처·재탐색 둘 다 동일하게 적용됨. 아래는 최초 요청 당시 기록.
- [ ] 요청됨 — 지금은 `source="manual"` 프로파일만 접속 성공 시 MAC을 캡처해서
  저장하는데, 고정 IP라 재탐색 대상이 아닌 시트 프로파일도 **MAC 주소 자체는
  미리 기록해두길** 원함. `ssh_session.py`/`mount_control.py`의
  `if profile.get("source") == "manual" and not profile.get("mac"):` 조건에서
  `source == "manual"` 체크를 빼고 MAC 캡처만 모든 프로파일에 적용하면 될 듯.
  ⚠ 확인 필요: 이건 "일단 기록만 해두자"는 건지, 아니면 **재탐색 로직도 시트
  프로파일에 적용할지**(현재는 재탐색 조건에 `source == "manual"`이 그대로 남아
  있어 시트 프로파일은 MAC이 있어도 재탐색은 안 걸림) 최종 결정 필요.


## 5. MAC 방식이 가끔 못 찾음 → 호스트 이름(mDNS) 방식도 2차 보험으로 구현
- [x] **구현 완료** — 처음엔 avahi가 꺼져있어서 실효성 없다고 판단해 보류했었는데,
  "MAC이 안 되면 mDNS로"라는 방향으로 다시 진행하기로 함 — 지금 당장은 avahi가
  꺼져있어도, 나중에 avahi가 켜지는 장비가 생기면 코드 수정 없이 바로 도움이 되는
  구조라 밑져야 본전이라고 판단.

  - 신규: `mdns_discovery.py` — `zeroconf` 패키지(pip install 필요, 새 의존성)로
    `resolve_mdns_hostname(hostname)` 구현. 내부적으로 `AddressResolverIPv4`를 써서
    `<이름>.local.` 형식으로 mDNS 조회, 응답 없으면(장비 꺼짐/avahi 비활성) None.
    ⚠ **PyInstaller 스펙(`SFTP_SSH_통합앱.spec`)에 새 의존성 추가 필요** — 다음
    빌드 때 `zeroconf`/`ifaddr`가 제대로 번들링되는지 반드시 확인해야 함(안 하면
    배포판에서 ImportError 날 수 있음).

  - `profile_store.py`: `get_profile()`에 `hostname` 필드 추가(MAC과 동일 패턴).

  - `ssh_session.py` `connect_profile()`: 접속 성공 시 `hostname` 명령 실행해서
    호스트 이름 캡처·저장(마운트 경로는 SSH 셸이 없어서 캡처 불가 — SSH 터미널을
    최소 한 번 열어야 채워짐). 재탐색 실패 조건도 `mac` 단독 체크에서
    `mac 또는 hostname` 체크로 수정 — 원래 MAC만 있으면 재탐색을 시도하던 걸
    hostname만 있어도 시도하도록 조건 확장(고치기 전엔 MAC 없이 hostname만 있는
    경우 mDNS 시도조차 안 하고 바로 실패하던 버그 있었음 — 테스트로 발견/수정).

  - `mount_control.py` `mount_profile()`: 동일하게 MAC 실패 시 mDNS로 재시도하는
    로직 추가(호스트 이름 캡처는 못 하지만 이미 저장된 값은 활용).

  - 순서: MAC 스캔 먼저 시도 → 실패하면 mDNS로 재시도 → 그것도 실패하면 기존처럼 에러.

  - **성공 경로까지 검증 완료** — 젯슨 없이도, 이 PC 자신이 `zeroconf`의
    `register_service()`로 가짜 mDNS 광고(`faketest2.local.` → 실제 IP)를 흉내내는
    방식으로 "avahi가 켜진 장비가 있는 상황"을 재현함. 틀린 IP로 등록한 프로파일이
    → 접속 실패 → MAC 없어서 건너뜀 → mDNS로 정확한 IP 찾음 → 프로파일 자동 갱신
    까지 `connect_profile()` 전체 경로로 end-to-end 확인됨 (2026-08-05). 실제
    젯슨(avahi 켜진 진짜 리눅스 장비)으로 최종 확인은 여전히 권장되지만, 코드
    로직 자체의 정확성은 이걸로 충분히 검증됨.

- [x] **도커 대역 필터링으로 해결됨 (2026-08-20)** — 아래에서 발견한 한계 자체는
  여전히 유효(여러 인터페이스가 있으면 mDNS가 틀린 응답을 줄 수 있음)하지만, 이
  앱의 실제 문제였던 "젯슨의 도커 브리지 주소가 후보에 섞여 매번 수동 확인이
  필요한 것"은 아래 구현 완료 항목으로 해소됨.
- [ ] **새로 발견한 한계 — 네트워크 어댑터가 여러 개인 장치에서는 mDNS 조회가 엉뚱한
  주소를 줄 수 있음** (2026-08-05, 수동 테스트 중 발견). 이 테스트 PC는 이더넷·와이파이·
  WSL 가상 어댑터 등 여러 개를 갖고 있는데, `ping -4 DESKTOP-ANDSEGG`로 자기 자신의
  호스트 이름을 조회했더니 실제로 바뀐 이더넷 IP(`192.168.111.46`)가 아니라 **WSL
  가상 어댑터의 주소(`172.30.144.1`)를 잘못 돌려줌.** 여러 인터페이스 중 어느 것의
  주소를 알려줄지 애매한 상황에서 mDNS/이름 조회가 틀릴 수 있다는 뜻.
  다만 실제 젯슨은 보통 네트워크 인터페이스가 하나뿐인 단순한 장치라 이 문제가 잘
  안 생길 가능성이 높음 — 이건 이 테스트 PC에 WSL이 깔려있어서 생긴 특수 상황일
  수 있으나, 실제 젯슨으로 검증할 때 한 번 더 확인이 필요한 부분으로 남겨둠.

  - ⚠ **실제 젯슨에서도 동일하게 확인됨 (2026-08-05)** — 받은 젯슨에서 `hostname -I`로
    확인해보니 `192.168.111.24`(진짜 이더넷 IP)와 `172.17.0.1`(도커 기본 브리지
    가상 주소, 낙상감지가 도커로 배포돼 있어서 생김)이 같이 나옴. 예상대로 "젯슨도
    도커 때문에 여러 주소를 가질 수 있다"는 우려가 실제로 확인됨 — mDNS/네트워크
    조회 결과에서 도커 주소가 섞여 나올 위험이 이론이 아니라 실제였음.
  - 📝 **(2026-08-20 재검토) 안전 문제가 아니라 UX 문제임을 재확인** — 실제 코드
    (`ssh_session.py`/`mount_control.py`)는 둘 다 `resolve_mdns_hostname_all()`(전체
    후보 반환)만 쓰고, 후보가 2개 이상이면 자동 선택 없이 무조건 경고 배너로 멈춘다
    (`mdns_ambiguity_state`). 첫 번째 후보를 그냥 골라버리는 `resolve_mdns_hostname()`
    (단일 버전)은 실제로는 **아무 데서도 호출되지 않는 죽은 코드**임을 grep으로 확인 —
    즉 "도커 주소를 잘못 골라 엉뚱한 장비에 조용히 연결"될 위험은 지금도 0%. 도커
    대역 필터링은 안전장치가 아니라, 후보 2개(이더넷+도커)가 뜰 때마다 매번 경고
    배너 + 사용자 확인을 요구하는 지금의 번거로움을 줄이는 **UX 개선**일 뿐임.
  - 📝 **(2026-08-20) 필터링 범위 실측 검증** — 처음 제안한 넓은 범위(`172.16.0.0/12`,
    도커가 쓸 수 있는 전체 사설 대역)로 필터링하면 위험함을 실제 `profiles.json`(68개
    프로파일)으로 확인 — **23개(1/3)** 원격지가 실제로 이 범위 안의 진짜 병동 IP를
    씀(`172.16.70.x`, `172.16.120.x` 등). 반면 도커가 실제로 쓰는 좁은 기본 브리지
    대역(`172.17.0.0/16`, 젯슨에서 확인된 `172.17.0.1`이 속한 범위)과 겹치는 실제
    원격지는 **0개** — 이 좁은 범위로만 한정하면 안전함이 확인됨.
  - [x] **(2026-08-20) 구현 완료** — `mdns_discovery.py`의 `resolve_mdns_hostname_all()`
    안에서, 응답이 **2개 이상일 때만** `DOCKER_DEFAULT_BRIDGE`(`172.17.0.0/16`)를
    걸러내고(필터링으로 다 사라지면 원래 목록을 그대로 유지) 반환하도록 수정. 후보가
    원래 1개뿐이면 필터를 아예 안 타므로, "미래에 어떤 병원이 하필 그 대역을 실제
    LAN으로 쓰는" 극단적 경우에도 유일한 정답을 잘못 걸러낼 위험이 없음(최악의 경우도
    "그 하나뿐인 후보를 그대로 신뢰"로 귀결). 호출부(`ssh_session.py`/`mount_control.py`)는
    `resolve_mdns_hostname_all()`을 그대로 재사용하는 구조라 안 건드림.
    - 검증: 실제 젯슨/네트워크 없이 `unittest.mock`으로 zeroconf 응답을 흉내내는
      방식으로 7개 시나리오 전부 통과 확인 — (1)도커 대역처럼 생긴 유일한 후보는
      그대로 유지 (2)이더넷+도커 IP 혼합 시 도커만 제거 (3)순서 무관 동일 동작
      (4)서로 다른 장비의 진짜 애매함은 안 건드림 (5)후보 전부 도커 대역인 극단적
      경우 원래 목록 유지 (6)응답 없음 (7)단일 버전(`resolve_mdns_hostname`)도
      필터링 결과 정상 상속.

- [x] **11번(nmap 전환)에서 사실상 해소됨** — 당시엔 핑(ICMP) 기반 ARP 캐시 채우기라
  ICMP가 막힌 구간에서 못 찾을 위험을 우려했으나, 11번에서 실제 재탐색 경로
  (`find_ip_for_mac`)가 nmap의 ARP discovery만 쓰도록 전환되면서 ICMP 의존 자체가
  없어짐(ARP는 ICMP와 무관한 별개 프로토콜). ping/TCP 기반이던 `_scan_once()`는
  현재 코드에 아무 데서도 호출되지 않는 죽은 코드로 남아있음(정리 대상). 아래는
  최초 발견 당시 기록.
- [ ] (참고, 별개 과제로 남겨둠) "가끔 못 찾음"의 또 다른 원인 후보 — `mac_discovery.py`가
  핑(ICMP)으로 ARP 캐시를 채우는 방식이라, ICMP가 막힌 구간에서는 장비가 멀쩡해도
  못 찾을 수 있음. 이번엔 mDNS를 보험으로 추가했지만, 근본적으로는 핑 대신 진짜
  ARP 요청을 직접 보내는 방식으로 바꾸는 것도 나중에 고려해볼 만함.

- [x] **범위 확장 완료** — `source == "manual"` 제한을 없애서, 시트에서 온 고정 IP
  프로파일도 접속 성공 시 MAC/호스트 이름을 캡처하고, 접속 실패 시 재탐색까지
  똑같이 시도하도록 변경(2026-08-05, 사용자 확인). "무한 재시도로 멈추는 위험"은
  기존 설계가 이미 "실패하면 원래 에러를 던지고 끝"(재귀 없음)이라 걱정 없음 —
  이미 있던 안전장치 그대로 유지됨. `ssh_session.py`/`mount_control.py` 양쪽 다
  캡처 조건과 재탐색 조건에서 `source` 체크를 제거함.


## 6. 앱 화면에서 MAC/호스트 이름/재탐색 상태를 확인·안내하는 기능
- [x] **가장 중요한 문제는 11번(nmap 전환)에서 해결됨** — `SFTP_SSH_통합앱.spec`이
  `console=False`로 빌드돼서 배포판엔 콘솔 창이 없고, 재탐색 관련 안내가 전부
  `print()`뿐이라 실제 배포된 앱에서는 IP가 자동으로 바뀌어 재접속됐는지 아무도 알
  방법이 없었던 문제 — `activity_log.py`(상태줄) / `ip_rediscovery_state.py`(배너)로
  해결됨.
- [x] **후보 3(원격지별 상태 표시)도 구현 완료** — 원격지 목록의 이름 옆에 "MAC 확보"
  (파란 배지) / "MAC 미확보"(회색 배지)를 표시. `profile_store.get_dynamic_ip_status()`
  (keyring 접근 없이 mac/hostname 확보 여부만 가볍게 반환) → `app_ui.Api.get_dynamic_ip_status()`
  → `index.html`의 `updateDynamicIpBadge()`가 렌더링 시 채움. `style-v4.css`에
  `.dip-badge` 스타일 추가(2026-08-20). ⚠ 이 배지는 "MAC이 저장돼 있는가"라는 정적
  상태만 보여줄 뿐, "지금 재탐색이 진행 중인가" 같은 동적 진행 상황은 여전히 위
  상태줄/배너가 담당 — 서로 역할이 겹치지 않음.
- [x] **후보 2(MAC 확인/복사 UI)는 8번의 "정보 수정" 모달로 사실상 충족됨** — 모달 안
  `ef_mac` 입력칸이 `get_profile_for_edit()`이 반환하는 MAC 값으로 채워져서, 목록에서
  바로 보이는 형태는 아니지만 파일을 직접 열지 않고도 확인/복사 가능.
- 아래는 최초 발견 당시 기록.
- `index.html`의 프로파일 목록 줄엔 지금 이름/마운트 상태(점)/볼륨 드롭다운/버튼만
  있고, IP·MAC·호스트 이름은 전혀 안 보임(`profiles.json`을 직접 열어야 확인 가능).

- 추가하면 좋을 후보 (우선순위 순):

  1. **재탐색 발생 시 상태줄에 안내 메시지** (예: "IP가 바뀐 것을 감지해 자동으로
     재연결했습니다 (192.168.1.10 → 192.168.1.23)") — `do_mount`/`get_remote_volumes`/
     `open_terminal_window` API가 지금은 재탐색 발생 여부를 프론트엔드로 전혀 안
     넘겨주므로, 반환값에 정보 추가 필요.

  2. **MAC 주소 확인/복사 UI** — 나중에 IT 담당자가 공유기 DHCP 고정 할당 설정할 때
     필요한 MAC을 파일을 직접 안 열어도 볼 수 있게.

  3. **"동적 IP 대응 상태"(MAC/호스트 이름 확보됨) 여부를 작은 표시로 안내.**


## 7. 엣지 케이스 — 저장된(오래된) IP가 "다른 원격지가 지금 쓰는 IP"와 우연히 겹치는 위험
- [x] **8번(7번 최종 대응)에서 사실상 해결됨** — 처음 제안했던 대응 아이디어(아래)는
  hostname 기반 신원 확인이었으나, 실제로는 8번 작업에서 hostname보다 신뢰도 높은
  **MAC 기반 신원 검증**(`mac_mismatch_state.py`, "원격지 이름 ↔ 기준 MAC" 쌍 대조)으로
  구현되면서 이 위험이 해소됨. 아래는 최초 발견 당시 기록.
- [x] **실제 젯슨 테스트 중 발견 (2026-08-05).** `Jetson_Test` 프로파일의 IP를 일부러
  예전 값(`192.168.111.46`)으로 바꿔서 재탐색 테스트하다가, 하필 그 주소가 **이번엔
  우리 테스트 PC(Windows OpenSSH 서버)가 쓰고 있던 주소와 겹쳐서** "Authentication
  failed"가 뜸 — 코드 버그는 아님(인증 실패는 의도적으로 재탐색 대상이 아니게 설계됨,
  엉뚱한 장비로 잘못 재탐색하는 걸 막기 위해서). 이번엔 우리 테스트 PC라 그냥 넘어갔지만:

  - **시트 데이터를 보면 거의 모든 실제 병동 젯슨이 같은 계정(`beclever`)과 상당수가
    같은 기본 비밀번호를 공유함.**
    
  - 그래서 실제 운영 환경에서 어떤 프로파일의 **저장된(오래된) IP가, 시간이 지나
    DHCP 재배정으로 완전히 다른 병동의 진짜 장비 IP와 겹치면**, 계정/비밀번호가
    우연히 통과해버릴 경우 **에러 하나 없이 조용히 엉뚱한 병동 장비에 접속해서
    파일을 만지게 되는 위험**이 있음 (인증 실패로도 안 걸러짐).

  - **대응 아이디어**: 이미 캡처해두는 `hostname`을 "신원 확인"용으로 활용 —
    접속 성공 후 저장된 `hostname`과 실제 접속한 장비의 `hostname`이 다르면
    "이 프로파일이 예상하던 장비가 아닌 것 같습니다" 경고를 띄우고 사용자 확인을
    받도록 하는 안전장치 추가를 검토할 만함. (아직 미구현, 검토만)


## 8. 7번 최종 대응 — 5가지 개선 (Sftp_Ssh_v2에서 작업 중, 별도 폴더)
- ⚠ 이 작업은 원본 `Sftp_Ssh`가 아니라 **`Sftp_Ssh_v2`(전체 복사본)에서 진행** —
  범위가 커서 안전하게 격리된 폴더에서 개발 후 나중에 반영하기로 함 (2026-08-05).

**MAC주소 시도**
- [x] **실제 서브넷 마스크 사용** — 무조건 `/24`로 가정하던 걸, `_get_local_prefix_length()`
  추가해서 PowerShell `Get-NetIPAddress`(언어 설정과 무관하게 항상 같은 필드명이라
  `ipconfig` 텍스트 파싱보다 안정적)로 이 PC가 실제 쓰는 서브넷 길이를 확인 후 그
  범위로 스캔. 확인 실패 시 기존처럼 `/24`로 대체. 대역이 너무 넓으면(/22 초과)
  최대 1024개까지만 스캔하도록 상한선도 추가. 실제 장비(MAC `5c-c0-a0-25-96-a0`)로
  테스트 완료 — 정상적으로 4.0초 만에 찾음.

- [x] **여러 번 재시도** — 한 번 스캔해서 못 찾으면 바로 포기하지 않고 `SCAN_RETRY_COUNT`(2회)
  만큼 다시 시도. 존재 안 하는 MAC으로 테스트 시 2번 재시도 다 돌고(8.4초) 정상적으로
  `None` 반환하는 것까지 확인 완료.

**Hostname 시도**
- [x] **호스트 이름 조회 시 후보가 여러 개면 자동으로 첫 번째 것만 쓰지 않고 안내**
  — `mdns_discovery.py`에 `resolve_mdns_hostname_all(hostname)` 신규 추가: 기존
  `resolve_mdns_hostname()`(첫 번째 주소만 반환)은 그대로 유지하되 내부적으로
  `_all()`을 호출하는 얇은 래퍼로 바꿈. `ssh_session.py`의 `connect_profile()`과
  `mount_control.py`의 `mount_profile()` 양쪽 다 재탐색 시 `_all()`로 바꿔 호출해서
  후보가 2개 이상이면 자동으로 고르지 않고 즉시 `RuntimeError`로 후보 IP 목록을
  전부 포함한 한국어 안내 메시지를 던지도록 변경(기존 에러 전파 경로를 그대로 타므로
  터미널/마운트 양쪽 다 사용자에게 바로 보임).

  - 실제 테스트로 검증 완료(2026-08-05): 이 PC 자신의 호스트 이름(`DESKTOP-ANDSEGG`)으로
    `resolve_mdns_hostname_all()`을 5회 반복 호출 — 매번 일관되게 두 개의 주소
    (`192.168.111.46`실제 이더넷, `172.16.254.78`가상 어댑터)를 함께 반환하는 것을
    확인, 5번 항목에서 발견했던 "여러 어댑터 있으면 mDNS가 첫 응답만 믿으면
    틀릴 수 있다"는 우려가 실제로 재현됨 — 이 기능이 해결하려는 문제 상황 자체가
    실존함을 재확인. 이어서 실제 호출부와 동일한 형태로 예외 메시지를 직접
    조립·발생시켜 최종 한국어 문구("mDNS 응답이 여러 개 발견되었습니다: 192.168.111.46,
    172.16.254.78 ... 자동으로 선택하지 않았습니다...")가 의도대로 출력되는 것까지 확인.

- [x] **원격지별 "정보 수정" 버튼 추가** — `profile_store.py`에 `get_profile_for_edit()`
  (모달 미리 채우기용), `app_ui.py`에 `get_profile_for_edit()`/`update_profile()` API 추가.
  `index.html`에 "+ 추가" 모달과 같은 구성의 "정보 수정" 모달 + 초록색 "수정" 버튼 추가
  (Ward/이름(고정)/호스트/계정/포트/인증방식/비밀번호 + MAC/호스트 이름까지 직접 확인·수정 가능).

  - ⚠ 테스트 중 발견한 버그: `save_profile()`을 그대로 재사용하면 접속 성공 시 자동
    캡처해둔 mac/hostname이 통째로 지워짐 — 수정 후 그 값들을 다시 복원하도록 수정,
    직접 재현 테스트로 확인(host/username/비번 바꿔도 mac/hostname 유지되는 것 확인).

  - host를 실제로 바꾸면(예전 장비 것일 수 있어 못 믿음) 모달의 MAC/호스트 이름 입력칸이
    자동으로 비워지는 힌트 동작 추가(강제 아님 — 알고 있으면 바로 다시 입력 가능, 비워두면
    다음 접속 성공 시 자동 재확보).

  - 수정 버튼 색을 회색(`btn-cancel` 재사용)에서 전용 초록 그라디언트(`btn-edit`)로 변경.

  - 삭제는 기존처럼 `confirm()` 확인창을 거쳐야만 실행됨(재확인 완료).

**앱창 내부**
- [x] **3번 기능(mDNS 후보 여러 개) 전용 경고 배너 + UX 다듬기** — 상태줄(1회성, 뒤이은
  메시지에 바로 덮어써짐)만으로는 놓치기 쉬워서, 메인 창에 계속 떠 있는 빨간 배너
  (`mdnsAmbiguousWarning`)를 추가. `mdns_ambiguity_state.py`(신규, 메모리 상태) —
  후보 2개 이상 발견 시 `mark()`, 재탐색 없이 정상 접속되거나 재탐색이 후보 1개로
  성공하면 `clear()`(자동 소멸). 상태줄에는 중복 방지로 짧게만("...아래 경고 배너를
  확인하세요") 띄우고 상세 내용은 배너로 일원화.

  - 후보 IP는 한 줄씩 줄바꿈해서 표시, 배너 전체 우측 상단에 X(닫기) 버튼 — 닫아도
    문제가 실제로 해소되면 자동으로 "닫았던 기록"도 지워져서 재발 시 다시 알림.

  - `webview.create_window()`에 `text_select=True` 추가 — pywebview 기본값이 `False`라
    텍스트 드래그 선택/복사가 아예 안 되던 게 CSS 문제가 아니라 이거였음 (사용자 확인 2026-08-05).

  - WebView2가 `style-v4.css`를 URL 그대로 캐싱해서 내용만 바꾸고 `?v=` 안 올리면 예전
    스타일이 계속 나오는 문제 재발 — `?v=12`→`16`까지 순차로 올리며 대응.

- [x] **7번 엣지케이스 2차 안전장치 — 원격지 이름 + MAC 기반 신원 검증** (사용자 제안,
  5번 항목보다 먼저 진행). hostname은 복제 장비끼리 겹칠 수 있어 못 믿지만, MAC은 물리
  장비에 붙어 절대 안 겹치므로 "원격지 이름 ↔ 기준 MAC" 쌍을 신뢰할 수 있는 신원표로 사용.

  - 신규 `mac_mismatch_state.py` — 접속 성공 시 새로 캡처한 MAC이 저장된 기준 MAC과
    다르면 `mark()`. `mdns_ambiguity_state`와 달리 **재접속해도 자동으로 안 지워짐**
    (저장된 기준값을 우리가 안 바꾸는 한 계속 다르게 캡처될 것이므로 "저절로 해소"는
    있을 수 없는 상황 — 사용자가 "수정"으로 직접 확인 후 반영해야만 해소).

  - `ssh_session.py`/`mount_control.py` 양쪽의 MAC 캡처 로직을 "없으면 저장"에서
    "있으면 비교, 다르면 경고(저장값은 안 덮어씀), 같으면 자동 clear"로 재구성.

  - **추가 아이디어(사용자 제안) 반영**: 불일치가 감지된 MAC이 `profiles.json`의 다른
    원격지가 이미 쓰던 기준 MAC과 같으면, "이 MAC은 이미 등록된 다른 원격지 'X'의
    기준 MAC과 같습니다"까지 알려주도록 `profile_store.find_profile_by_mac()` 추가—
    "다르다"에서 그치지 않고 실제로 어느 장비인지 바로 짚어줌.
    
  - 빨간 배너(`macMismatchWarning`, 🚨) — 저장된 MAC(예전 기준)과 지금 연결된 장비의
    MAC을 각각 보여줌. `update_profile()`(정보 수정 저장)로 반영하면 예전 경고는
    초기화되고 다음 접속에서 다시 검증됨.

  - 검증: 실제 접속 없이 `unittest.mock.patch`로 `ssh_session._connect`/
    `mac_discovery.get_mac_for_ip`를 모킹해서 (1)최초 캡처(그냥 저장, 불일치 아님)
    (2)불일치(저장값 안 바뀜, 배너 상태에 기록됨) (3)재일치(자동 clear) (4)다른
    원격지 MAC과 겹치는 경우(`actual_mac_owner` 정상 포함) 네 가지 시나리오 모두
    스크립트로 직접 실행해 확인 (2026-08-05). 실제 Jetson_Test로도 검증 예정(현재
    borrowed 장비가 네트워크에 없어 보류 — 저장된 MAC만 가짜 값으로 바꿔서(장비는
    안 건드림) 테스트하는 방법을 사용자와 합의함).

  - **초록 확인 배너 추가** (`macResolvedInfo`, ✅) — 불일치였다가 다음 접속에서
    실제로 일치로 돌아오는 "전환" 시점에만 뜬다(매번 일치할 때마다 스팸처럼 뜨지
    않도록 전환 감지). 신규 `mac_resolved_state.py`. X로 닫을 수 있음(빨간 배너들과
    같은 우측 상단 X 패턴 재사용). **"수정" 모달로 직접 고쳐서 저장하는 경로**도
    지원 — 저장하는 MAC 값이 불일치 당시 실제로 캡처됐던 MAC(`actual_mac`)과 같으면
    "확인하고 받아들임"으로 간주해 초록 배너, 그냥 다른 값을 넣으면(예: host를 완전히
    다른 장비로 바꾼 경우) 확인 표시 없이 조용히만 지움. 재접속 경로/수정 저장 경로
    둘 다 스크립트로 시나리오별 검증 완료.

- [x] **시트-로컬 불일치 안내** (사용자 제안, 부가 기능) — MAC/mDNS 재탐색으로 시트에서
  온(`source="sheet"`) 프로파일의 host가 로컬에서만 자동 교정되면, 구글 시트 자체는
  건드리지 않기로 했으므로(전체 사용자에게 영향 주는 변경이라 — 사용자 확인 2026-08-05)
  다음 "↻ 시트 새로고침" 때 시트의 예전 값으로 되돌아갈 수 있음을 미리 알려주는 안내.

  - 신규 `sheet_diverged_state.py` — 재탐색 성공(재접속까지 검증된 시점) 시
    `mark(name, sheet_host, local_host)`. `sheet_sync.py`의 `sync_from_sheet()`가
    매 행을 처리할 때마다(값이 바뀌었든 이미 같아서 안 바뀌었든) `clear()`를 호출해서
    다음 동기화 시점에 자동으로 해소되도록 함.

  - UI는 다른 배너들과 다르게 **가운데 정렬, X 닫기 버튼 없음**(다음 시트 동기화로만
    해소됨 — 사용자 요청). 색은 심각한 신원 위험 배너(mDNS 후보/MAC 불일치, 진한 빨강
    `#fbe0e0`/`#9c2b2b`)와 구분하기 위해 "시트 관련 안내" 계열인 옅은 빨강
    (`#fdecec`/`#b23a3a`, `sheet-issue-warning`과 동일 팔레트)으로 통일.

  - 검증: `unittest.mock`으로 재탐색 성공 시나리오를 재현해 `mark()`가 정확한
    시트값/로컬값으로 기록되는 것 확인, 이어서 `sync_from_sheet()`를 가짜 시트 행으로
    실행해 `clear()`되고 실제로 host가 시트값으로 되돌아가는 것까지 end-to-end로 확인
    (2026-08-05) — "다음 새로고침 때 로컬 교정이 되돌려질 수 있다"는 우려 자체가
    실제로 재현됨을 검증.

- [x] **5번 기능 — 중복 IP 경고** — `profile_store.py`에 `find_duplicate_host_groups()`
  추가. 여러 단계로 좁혀나감(전부 사용자 확인/제안, 2026-08-06):
  1. 처음엔 host만 비교 → us.loclx.io처럼 터널 하나를 여러 원격지가 포트만 다르게
     나눠 쓰는 정상 구성까지 오탐으로 잡히는 걸 실제 데이터로 확인 → host+port로 수정.

  2. Ward가 다르면 물리적으로 같은 LAN에 동시에 있을 일이 거의 없어 위험이 크게
     낮아진다는 논의 끝에 → host+port+ward가 모두 같을 때만 경고하도록 좁힘.

  3. SSH는 `username@host`라 username이 다르면 인증 단계에서 걸러져 애초에 "성공"으로
     안 붙는다는 점에 착안 → host+port+ward+username까지 전부 같을 때만 진짜 위험으로
     최종 확정.

  - 앱 상단(Ward 무관, 항상 표시)에 노란 배너로 "계정@IP:포트 — 원격지들" 나열, 시트
    확인 권장 문구 포함. 앱 시작/추가/수정/삭제/시트 동기화/재접속 등 profiles.json이
    바뀔 수 있는 모든 시점에 갱신되도록 연결.

  - 실제 profiles.json으로 검증: 처음엔 오탐 포함 3건 감지 → host+port만으론
    loclx 9개 원격지가 잘못 걸림(수정) → ward 조건 추가 후 진짜 2건으로 좁혀짐 →
    username 조건까지 추가해도 최종 1건 그대로 유지되는 것 확인(그 1건은 실제로
    테스트 중 남은 잔재 데이터).

## 9. 배포 후 WebView2 캐시 문제 예방 (실사용 중 발견)
- [x] **"수정"으로 바꿨는데 `profiles.json`엔 예전 값이 남아있는" 문제 발생** —
  원인 조사 결과 `update_profile()` 로직 자체는 정상(스크립트로 직접 재현 시 문제
  없음). 실제 원인은 **WebView2가 `webview_data`에 index.html/JS를 캐싱**해서, 파일을
  고쳐도 앱을 재시작할 때마다 예전 버전의 JS가 계속 로드되고 있었던 것 — 이 프로젝트에서
  전에도 겪었던 문제가 재발함. 수동으로 `webview_data` 폴더 이름을 바꿔서 캐시 무력화
  후 문제 해소 확인.

- [x] **배포 후에도 같은 문제가 재발할 수 있다는 우려 → 재사용 가능한 해결책 구현.**
  `app_ui.py`에 `APP_BUILD_VERSION` 상수 추가 — 배포할 때마다 이 숫자만 올리면 됨.
  앱 시작 시 마지막 실행 버전(`webview_data_version.txt`)과 비교해서 다르면(=새로
  배포된 것) 그때만 `webview_data`를 지우고 새로 씀. 같은 버전으로 재실행할 땐 안
  건드려서, `storage_path`를 고정해둔 원래 목적(재시작마다 새로 초기화하면 창 뜨는 데
  ~10초 걸리던 문제 방지)은 그대로 유지됨 — 새 버전 배포 직후 첫 실행 한 번만 느려짐.
  임시 폴더로 격리해서 스크립트로 로직 검증(마커 없음→지워짐, 같은 버전→유지, 버전
  변경→지워짐), 실제 앱 재시작으로도 버전 파일이 올바르게 갱신되는 것까지 확인.

  - ⚠ URL에 `?v=` 쿼리를 붙이는 방식(CSS에 쓰던 방식)은 메인 `index.html`엔 적용 불가 —
    pywebview가 로컬 파일 경로를 `os.path.relpath()`로 처리해서 쿼리스트링이 붙으면
    파일을 못 찾게 됨(pywebview 소스 확인함). 그래서 버전 마커 파일 방식으로 대신함.

## 10. 실사용 중 발견 — MAC 조회가 라우팅/터널 경유 원격지에서 무력화되는 사각지대
- [x] **실제로 재현된 위험 사례**: `Jetson_Test`가 원래 알려진 로컬 IP(192.168.111.7,
  와이파이 꺼짐)가 아니라 이 PC의 로컬 대역(192.168.111.0/24) 밖에 있는
  `192.168.120.12`(라우팅으로 도달, hostname이 "beclever-desktop"이라 실제 젯슨이
  아닌 것으로 추정)로 마운트/접속에 "성공"했는데도, MAC 불일치 경고가 전혀 안 뜨는
  문제 발견 (2026-08-06). 원인 조사 결과: **ARP는 같은 로컬 네트워크(L2) 안에서만
  동작하는 프로토콜이라, 라우터/VPN/터널(loclx.io 포함)을 거쳐 도달하는 원격지는
  `mac_discovery.get_mac_for_ip()`가 애초에 MAC을 못 알아냄(항상 None)** — 그래서
  비교 자체가 시도조차 안 되고 조용히 넘어가고 있었음. 7번 안전장치의 사각지대였음.

- [x] **해결**: `ssh_session.py`에 `get_remote_mac(client)` 신규 추가 — ARP처럼
  "내 PC 기준 밖에서 알아내는" 방식 대신, 이미 맺어진 SSH 세션으로 **그 장비한테
  직접 자기 MAC을 물어보는** 방식(`ip route show default`로 기본 라우트 인터페이스를
  찾은 뒤 `cat /sys/class/net/<iface>/address`)으로 바꿈 — 로컬/라우팅/터널 상관없이
  항상 동작함. 도커 veth 등 잡음 있는 인터페이스가 섞이는 문제(이 세션에서 실제
  젯슨으로 확인된 이슈)를 피하려고 "기본 라우트로 나가는" 인터페이스 하나만 고름.
  `connect_profile()`에서 이 방식을 우선 시도하고, 실패하면(방화벽 등 드문 경우)
  기존 ARP 방식으로 대체(`get_remote_mac(client) or mac_discovery.get_mac_for_ip(...)`).

  - ⚠ `mount_control.py`(rclone 경로)는 SSH 셸이 없어서 이 방식을 못 씀 — ARP만
    가능하므로, 터미널을 한 번도 안 연 채 마운트만 한 라우팅/터널 원격지는 여전히
    사각지대로 남음(주석으로 명시해둠). 최소 한 번 터미널로 접속하면 그때
    ssh_session.py 쪽에서 확실히 캡처됨.

  - 검증: 실제 상황(ARP 실패 + 라우팅된 주소)을 그대로 재현하는 목업 테스트로
    확인 — ARP가 실패해도(None) `get_remote_mac()`이 SSH로 성공해서 불일치를 정상
    감지함, 그리고 `get_remote_mac()`이 실패하는 경우엔 기존 ARP 대체 경로가 여전히
    동작하는 것도 별도로 확인 (2026-08-06).

## 11. 동적 IP 재탐색 방식을 순수 ARP 핑 스윕에서 nmap 기반으로 전환 (2026-08-12 ~ 08-18)
- [x] **근본 원인 수정 — 재탐색이 실패하던 진짜 이유** — IP가 막 바뀐 직후의 장비는
  ARP 해석부터 새로 해야 해서 첫 응답이 유독 느린데(같은 장비가 첫 ping 255ms →
  두 번째부터 4ms), 대역 전체 응답 시간을 실측해보니 살아있는 47개 중 6개(13%)가
  300~1000ms 구간이라 기존 300ms 타임아웃에 통째로 잘려나가고 있었음. 젯슨이 IP를
  막 바꾼 직후가 정확히 이 구간이라 "멀쩡히 네트워크에 있는데도 못 찾음"으로
  오판되던 것 — 프로브 타임아웃 300ms → 1000ms로 늘리고 ping/TCP를 순차가 아니라
  동시 전송, 워커도 40 → 96으로 늘려 상쇄 (`mac_discovery.py`).

- [x] **스캔 동시 수 튜닝(실측 기반)** — 반대로 동시 요청이 지나치게 많으면 오히려
  대상을 놓치는 것도 확인됨(96동시=36~48개, 48동시=44~52개, 32동시=51~59개,
  24동시=51~52개 검출 — 동시 수를 줄일수록 ARP 수집률 상승). 1차는 빠르게(96)
  훑어 흔한 경우를 잡고, 놓치면 2차는 촘촘하게(32) 다시 훑는 2단계 스캔으로 절충.

- [x] **nmap 기반 탐색 도입** — 자체 ping/TCP 스윕 대신 nmap의 ARP discovery를
  사용하도록 전환(v4 전용, 이전 브랜치에는 반영 안 함 — 사용자 확인 2026-08-12).
  `nmap_check.py` 신규: PATH에 nmap이 없으면 최초 1회만 winget 자동 설치 여부를
  물어보고, 이후는 flag 파일로 재확인 생략. `SFTP_SSH_통합앱.spec`에도 배포 시
  누락되지 않도록 반영 필요(빌드 시 확인 대상).

- [x] **재접속 진행 상황을 콘솔이 아니라 앱 화면에 노출** — 배포판은 `console=False`라
  콘솔이 없어서, 재탐색/재접속 진행 상황이 지금까지 전혀 안 보였던 문제(6번 항목의
  "가장 중요한 문제"로 지적됐던 부분)를 실제로 해결. 신규 `activity_log.py` — 앱
  창 하단 상태줄에 "같은 주소로 재시도 중", "새 IP 발견" 등을 흘려보내고, 20초
  지나면 자동으로 목록에서 빠짐. 기존 배너 폴링(7초)과 별도로 2초 주기로 확인.
  신규 `ip_rediscovery_state.py` — 재탐색 진행 중(노란 배너)/재연결 성공(초록
  배너) 표시, 기존 배너 폴링에 얹어서 추가 부하 없음.
  
- [x] **터미널 자동 재연결 강화** — 끊김 감지 주기 2초, 끊기면 5초 간격으로 최대
  10회까지 자동 재시도(사용자가 재접속 버튼을 반복해서 누를 필요 없어짐). 원래는
  10초/15초부터 시작해 점점 늘리는 방식이었으나, 실사용 측정 결과 재시도 "시도"
  자체가 이미 15초쯤 걸려서(초기 접속 5초 + 같은 주소 재시도 4초 + MAC 스캔) 대기까지
  길게 잡으면 전체 복구가 60초 가까이 걸리는 문제가 있어 고정 5초 간격으로 단축
  (2026-08-11). 이후 재접속 시도 중 취소 흐름도 개선(2026-08-14).
- [x] **새 IP 재접속 재시도 횟수 하향 조정** — `NEW_IP_CONNECT_RETRY_COUNT`를 5(최초 1회
  + 재시도 4회) → 3(최초 1회 + 재시도 2회)으로 줄여 과도한 재시도로 지연되던 것을 줄임
  (2026-08-18, `ssh_session.py`).
- [x] **백그라운드 점검 범위 제한** — 이 PC와 다른 네트워크 대역에 있는 원격지(다른
  병원 장비)는 백그라운드 상태 점검 대상에서 제외. 67개 전부를 훑느라 한 사이클에
  402초(6.7분)가 걸리고 그동안 전역 스캔 락과 네트워크를 점유해 정작 사용자
  재접속이 4.9초 → 14.35초로 밀리던 문제 해결(대상 67개 → 4개, 사이클 402초 →
  약 19초로 개선, `background_watcher.py`).
- [x] **실제 Jetson 장비로 end-to-end 검증**: 스캔 3/3 성공(9초), 죽은 IP →
  자동 복구 15.8초, 마운트 3.6초, 백그라운드 사이클 24.9초 확인.
- 📝 별도 검토 문서로 정리됨: [동적IP_재탐색_개선안_v4.md](동적IP_재탐색_개선안_v4.md)
  (stale IP — 방금 실패한 후보를 얼마나 믿을지에 대한 개선안 검토, 2026-08-13),
  [테스트스크립트_StaleIP_로직.md](테스트스크립트_StaleIP_로직.md)(2026-08-14).
  결론은 "완전 제외"가 아니라 "실패 후보 우선순위만 낮추는" 보수적 방식 채택 —
  아직 코드에는 미반영, 다음 세션 검토 대상으로 남음.

## 12. Ward 전환 시 원격지 개수만큼 SSH 접속이 동시에 몰리는 문제 (2026-08-20 발견)
- [ ] **발견 경위**: Ward를 눌러 원격지 목록이 뜨는 순간, 터미널에 접속 시도 로그가
  한꺼번에 우르르 찍히는 게 실사용 중 눈에 띔.
- **원인**: `index.html`의 목록 렌더링 루프가 원격지마다 `loadVolumeOptions(name)`을
  `await` 없이 호출함 — Ward에 원격지가 20개면 `get_remote_volumes()`(=SSH 접속 +
  `df -hP` + 접속 종료)가 사실상 동시에 20번 발생함(pywebview가 js_api 호출을 각각
  별도 스레드로 처리해서 실제로 병렬 실행됨).
  - `updateMountStatus()`/`updateDynamicIpBadge()`는 `mount_state.json`/`profiles.json`을
    로컬에서만 읽어서 네트워크를 안 타므로 이 문제와 무관함 — 부담은 오직
    `loadVolumeOptions()`(볼륨 드롭다운 채우기)에서만 발생.
- **연결 자체가 누적되어 쌓이는 메모리 누수는 아님** — `get_remote_volumes()`는
  `finally: client.close()`로 매번 확실히 정리함. 문제는 "동시에 몰리는 순간의 부하"임.
- **최악 시나리오**: 그 Ward에 꺼진 장비가 여러 개 섞여 있으면, 각각 접속 실패 →
  재시도(최초 1회 + 재시도) → 그래도 실패하면 MAC 기반 nmap 재탐색까지 시도함. nmap
  스캔은 `_scan_lock`으로 한 번에 하나씩만 돌게 막혀있어 순서대로 줄은 서지만, 꺼진
  장비가 많은 Ward를 열면 스캔이 줄줄이 쌓여 한동안 CPU/네트워크를 계속 붙잡을 수 있음.

**검토한 개선안**

1. **볼륨 목록을 지연 로딩(lazy load)으로 전환** — 렌더링 시 미리 다 채우지 않고,
   사용자가 그 원격지의 볼륨 드롭다운을 실제로 클릭할 때만 `get_remote_volumes()` 호출.
   - 장점: 화면에 목록을 띄우는 순간에는 SSH 접속이 하나도 안 발생 — 사용자가 실제로
     관심 있는 원격지만 접속을 타므로 부하가 사실상 사라짐.
   - 단점: 드롭다운을 처음 열 때 "조회 중..." 표시 후 약간의 지연이 생김(기존엔
     미리 채워져 있었음). UX가 살짝 달라짐.
   - 판단: **가장 간단하고 효과가 확실함.** 마운트 상태 점/MAC 배지는 이미 로컬
     조회라 영향 없이 그대로 즉시 표시됨.
2. **동시 실행 개수 제한(throttling)** — 지금처럼 렌더링 시 다 채우되, 한 번에
   N개(예: 4~6개)씩만 순차로 처리하고 나머지는 대기열에 쌓기.
   - 장점: 기존 UX(목록 뜨자마자 볼륨이 다 채워져 있음) 그대로 유지.
   - 단점: JS 쪽에 큐/세마포어 로직을 새로 짜야 함 — 1번보다 구현이 복잡함. 원격지가
     많은 Ward는 여전히 전체적으로 다 채워지기까지 시간이 걸림(동시 처리만 줄 뿐).
   - 판단: 효과는 있지만 1번 대비 구현 비용 대비 이득이 적음.
3. **아무것도 안 함(보류)** — 지금까지 실사용 중 앱이 실제로 멈추거나 느려졌다는
   증상 보고는 없음(터미널 로그만 몰릴 뿐). 원격지 30개 미만인 Ward가 대부분이라
   실질적 위험은 제한적일 수 있음.
   - 판단: 꺼진 장비가 많은 Ward가 실제로 존재하는지, 그 Ward를 열 때 체감 지연이
     있는지 먼저 확인 후 결정하는 게 안전.

**추천**: 1번(지연 로딩)이 구현 난이도 대비 효과가 가장 크지만, 아직 실사용 중
체감 문제(멈춤/느려짐)로 이어졌다는 보고는 없어서 **긴급하지는 않음** — 다음
세션에서 실제 Ward별 원격지 수·꺼진 장비 비율을 확인한 뒤 착수 여부를 정하는
것을 권장.
