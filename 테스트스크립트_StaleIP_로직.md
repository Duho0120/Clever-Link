# 테스트 스크립트 Stale IP 로직 메모

작성일: 2026-08-14

## 목적

동적 IP 변경 직후, 공유기/AP 또는 Windows ARP 캐시가 이전 IP를 계속 목표 MAC처럼 보여주는 상황을 줄이기 위한 테스트용 로직이다.

## 현재 앱 로직 요약

1. 기존 IP로 SSH 접속 시도
2. 실패하면 nmap 재탐색을 백그라운드에서 시작
3. 동시에 기존 IP로 짧게 1회 재시도
   - 1초 대기
   - 최대 3초 접속 시도
4. 기존 IP 재시도 실패 시 nmap 결과를 기다림
5. MAC이 맞는 IP를 찾으면 해당 IP로 SSH 접속 최대 3회 재시도 (2026-08-18 기준, 원래 5회에서 축소됨)
6. MAC으로 못 찾으면 mDNS fallback

특징:

- MAC이 맞는 IP를 쉽게 버리지 않는다.
- 실제 정답 IP인데 sshd/Wi-Fi가 늦게 뜨는 경우에 안전하다.
- 단, 낡은 이전 IP가 MAC 일치로 계속 보이면 그 IP에 시간이 묶일 수 있다.

## 테스트 스크립트 로직 요약

1. nmap으로 저장된 MAC과 일치하는 IP를 찾는다.
2. 찾은 IP로 SSH 접속을 시도한다.
3. MAC은 맞지만 SSH 접속이 실패하면 stale 후보로 기록한다.
4. stale 후보는 약 20초 동안 우선순위를 낮추거나 제외한다.
5. 다음 탐색에서 다른 MAC 일치 IP가 보이면 그 IP를 먼저 시도한다.
6. 목표 IP가 잡혀 SSH 연결될 때까지 반복한다.

특징:

- 이전 IP가 낡은 ARP/MAC 정보로 계속 보이는 상황에서 한 IP에 묶이지 않게 한다.
- 최악 시간을 줄일 가능성이 있다.
- 하지만 실제 정답 IP인데 SSH만 늦게 뜬 경우에도 뒤로 밀 수 있어 배포 로직으로는 조심해야 한다.

## 핵심 차이

현재 앱:

```text
MAC이 맞으면 정답일 수 있으므로 여러 번 재시도한다.
```

테스트 스크립트:

```text
MAC이 맞아도 SSH가 안 되면 낡은 후보일 수 있으므로 잠깐 의심하고 다른 후보를 먼저 본다.
```

## 앱 반영 시 안전한 형태

테스트 스크립트 방식을 그대로 넣기보다는 다음처럼 완화해서 넣는 것이 안전하다.

```text
1. SSH 실패 IP를 완전 폐기하지 않는다.
2. 프로파일별 failed/stale 후보로 기록한다.
3. 같은 MAC 후보가 여러 개면 실패 후보를 뒤로 미룬다.
4. 후보가 실패 후보 하나뿐이면 계속 재시도 가능하게 둔다.
5. 같은 실패 후보가 반복되면 다른 후보 탐색을 한 번 더 우선한다.
```

## 보류 이유

20초 완전 제외는 다음 케이스에서 위험할 수 있다.

```text
실제 정답 IP는 맞음
하지만 Jetson Wi-Fi 또는 sshd가 아직 준비되지 않음
SSH banner timeout 또는 socket timeout 발생
```

이 경우 정답 IP를 20초 동안 뒤로 밀면 오히려 재접속이 늦어질 수 있다.

## 현재 판단

- 배포 안정성: 현재 앱 로직이 더 안전하다.
- 최악 시간 단축 가능성: 테스트 스크립트 로직이 더 크다.
- 추천 반영 방식: 완전 제외가 아니라 우선순위 하향 방식.
