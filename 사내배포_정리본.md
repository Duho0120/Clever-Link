# SFTP/SSH 통합 접속 도구 — 사내 배포 정리본

**배포 결정**: 사내 전용 툴이며, 구글 시트 링크가 외부로 유출될 가능성이 낮다고 판단하여 보안/유연성 개선안(연동시트 변경, 서비스 계정 인증)은 이번 배포에는 포함하지 않고 지금 버전 그대로 사내 배포를 진행하기로 확정.

---

## 1. 이 도구가 하는 일

RaiDrive 대체 목적의 Windows 전용 앱. SFTP 드라이브 마운트(rclone + WinFsp)와 SSH 터미널 접속(paramiko + xterm.js)을 pywebview 창 하나로 통합. 병원별로 흩어진 ~130여 개 서버(Jetson 기기)를 병동(Ward)/Room 단위로 묶어서 관리.

## 2. 핵심 기능

### 2-1. 기본 접속 관리
- 원격지(서버) 등록 → 자동 마운트, 드라이브 문자 자동 배정/재사용
- 서버 쪽 볼륨(`df -hP` 조회) 드롭다운으로 원하는 경로 선택 마운트
- 별도 창의 SSH 터미널(xterm.js), 세션 여러 개 동시 지원
- 앱을 창 X로 닫아도 마운트/터미널 유지 (트레이 아이콘에서만 완전 종료)

### 2-2. Ward/Room 계층 구조
- Ward(병동)는 실제 원격지가 아니라 화면 필터 역할, Room이 진짜 원격지
- 같은 병원 이름이라도 동별로 다른 Ward로 구분 가능 (예: "포항세명기독병원(62)")
- Ward를 고르기 전엔 원격지 목록이 비어있음 (~130개를 한 번에 안 쏟아냄)

### 2-3. 구글 시트 자동 연동
사내 구글 시트(기기명/Ward/Room/Host name/IP/ID/PW 컬럼)에 입력만 하면 자동으로 원격지로 등록됨.

- **원격지 이름 = `Room-HostName`** 조합 (예: `3-co52`) — Room 번호만으로는 같은 Ward 안에서도 겹칠 수 있어서, Host name(고유함)까지 합쳐서 이름 충돌 방지
- **로컬 IP + 터널 주소 동시 지원**: 한 원격지에 기본 주소(사내망)와 보조 주소(외부 터널)를 같이 저장해두고, 실제 접속하는 순간 `host_resolver.py`가 짧게(1.5초) 살아있는 쪽을 자동으로 골라 연결
- **Room/Ward 병합 셀 처리**: 시트에서 셀이 병합되어 있으면(값이 첫 행에만 있고 나머지는 빈칸) 위 행 값을 이어받되, Ward 경계에서는 반드시 초기화 (다른 병동 값이 잘못 넘어오는 걸 방지)
- **같은 행 안 여러 Room**("401\n402")은 분할 등록이 아니라 **쉼표로 합쳐 원격지 1개**로 등록 (한 기기가 방 여러 개를 담당하는 경우이기 때문)
- 괄호로 감싸인 설명 메모(예: `"(lobby, hall)"`)는 Room으로 오인해 분할하지 않고 무시
- IP 칸의 `주소:포트` 형식 자동 분리, PW 빈칸이면 공통 기본값 사용
- Room/IP/Ward/ID 중 필수 정보가 없어 등록 못 한 행은 앱을 막지 않고 Ward별로 개수를 세어 빨간 경고 문구로만 안내
- **유령 프로파일 감지**: 시트에서 사라진 원격지(`source="sheet"`)는 자동 삭제하지 않고 화면에 경고 + [삭제] 버튼으로만 표시 — 실수로 행이 잠깐 지워져도 데이터가 순식간에 날아가지 않도록
- 앱에서 직접 추가한 원격지(`source="manual"`)는 시트 동기화 대상에서 제외되어 안전
- **자동 폴링(주기적 새로고침) 제거**: "↻ 시트 새로고침" 버튼을 눌렀을 때만 동기화 (사용자 결정)
- 변경 없는 프로파일은 재저장하지 않음 (불필요한 파일/자격증명 쓰기 방지)

### 2-4. 안정성/진단 개선
- `mount_control.py`의 마운트 실패 메시지가 로컬 rclone 통신 에러(`127.0.0.1:5572...`)만 보여주던 것을 → 실제 접속을 시도한 목적지 주소와 알아보기 쉬운 한국어 사유로 교체
- WinFsp / WebView2 Runtime 미설치 시 자동으로 설치 여부를 물어보고 winget으로 설치 (최초 1회만 확인, 이후 생략)

## 3. 실제 데이터로 검증하며 찾아낸 것들

이번 시트 연동 개발 과정에서 실제 회사 시트를 붙여보며 아래 문제들을 발견/확정함 (전부 시트 데이터 문제였지 앱 로직 문제는 아니었음):

- **명은 병동 6개 기기**: 터널 주소가 `us.` 접두사 없이 `loclx.io`로만 적혀 있어 연결 불가 — 실제로 `us.loclx.io:같은포트`로 접속하면 정상 작동함을 확인, 시트 수정만 하면 해결
- **원주보훈요양원 3개 기기**: 주소는 정상이지만 실제로 회수(철거)된 기기라 연결 안 되는 게 맞는 상태
- **Atman AI(co50) 등**: Room/IP가 비어있는 건 그 행 자체가 원래 미완성/회수된 기기라서 그런 것
- 나머지 로컬 IP 전용 기기 다수는 그 병원 현장/VPN에서만 접속 가능한 정상 구조

## 4. 배포 방식

- PyInstaller `--onedir` 빌드 (`SFTP_SSH_통합앱.spec`) — `SFTP_SSH_통합앱.exe` + `_internal` 폴더를 통째로 zip으로 압축해 전달
- 배포 전 개발자 개인 테스트 흔적(`known_hosts_launcher`, `webview_data` 캐시, `profiles.json`, 설치 확인 플래그 파일) 전부 초기화 — 직원 PC에서 WinFsp/WebView2 설치 여부를 실제로 새로 확인하도록 함
- 직원 안내사항: 압축 해제 후 `SFTP_SSH_통합앱.exe` 실행 → Windows 게시자 미확인 경고 시 "추가 정보 → 실행" → 최초 실행 시 "↻ 시트 새로고침" 클릭 필요

## 5. 이번 배포에 포함하지 않은 것 (개인 프로젝트로 이어감)

- **보안 개선**: 구글 시트 공개 링크(누구나 링크로 열람 가능) → 서비스 계정 인증 방식 전환. 사내 전용 툴이고 링크 유출 가능성이 낮다고 판단해 이번엔 보류
- **유연성 개선**: "연동시트 변경" 기능(URL/키를 앱 안에서 직접 교체, 여러 시트를 목록으로 저장해 드롭박스로 전환)
- **부가**: winget 설치 시 터미널 창이 잠깐 뜰 수 있는 문제(`CREATE_NO_WINDOW` 미적용), Ward 드롭박스 개인 Ward 최상단 고정, 정식 설치 프로그램(Inno Setup) 등

→ 위 항목들은 [체크리스트.md](체크리스트.md)에 계획으로 남겨뒀고, 이번 사내 배포와는 별개로 개인 프로젝트로 계속 진행 예정.
