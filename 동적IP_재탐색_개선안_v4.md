# 동적 IP 재탐색 개선안 v4

작성일: 2026-08-13

## 현재 결론

현재 v4의 nmap 6슬롯 탐색 방식은 "새 IP가 네트워크에 보이는 순간"에는 꽤 빠르게 잡는다. 실제 테스트에서도 슬롯 앞쪽 IP는 3~12초대까지 나왔다.

다만 오늘 테스트에서 확인한 것처럼, 느려지는 원인은 크게 세 가지로 나뉜다.

1. 목표 IP가 현재 6슬롯 탐색 순서상 뒤쪽에 있음
2. 이전 IP가 ARP/MAC 후보로 계속 남아 먼저 잡힘
3. 새 IP 자체가 공유기/AP/Jetson Wi-Fi 상태 때문에 한동안 ARP/nmap에 드러나지 않음

1번은 슬롯 탐색 구조의 트레이드오프다. 3번은 앱에서 근본적으로 해결하기 어렵다. 앱에서 개선 여지가 있는 부분은 주로 2번이다.

## 문제 상황

Jetson의 실제 IP가 바뀌었는데 PC 또는 공유기/AP가 한동안 이전 ARP 정보를 유지할 수 있다.

예:

```text
실제 Jetson IP: 192.168.111.134
PC/ARP에서 계속 보이는 후보: 192.168.111.55
MAC: ac-3a-e2-7c-18-ba
```

이 경우 앱은 MAC 기준으로 55를 "우리 장비"라고 판단할 수 있다. 하지만 실제 SSH 연결은 실패하고, 다음 재탐색에서도 다시 55를 먼저 잡으면 시간이 크게 늘어난다.

반대로 주의해야 할 케이스도 있다.

```text
실제 Jetson IP: 192.168.111.55
MAC도 맞음
하지만 Wi-Fi/sshd가 아직 불안정해서 SSH만 잠깐 실패
```

이때 55를 바로 제외하면 정답 IP를 버리는 셈이라 오히려 재접속이 늦어질 수 있다.

## 검토한 개선안

### 1. Stale IP 완전 제외

방금 찾은 IP로 SSH 연결이 실패하면 그 IP를 10~20초 동안 탐색 결과에서 제외한다.

장점:

- 이전 IP만 반복해서 잡는 최악 케이스를 빠르게 끊을 수 있다.
- 테스트에서 stale 후보가 여러 번 반복되는 상황에는 효과가 있었다.

단점:

- 그 IP가 실제 정답인데 SSH 서버만 늦게 뜬 경우, 정답 IP를 잠깐 버릴 수 있다.
- 직원 배포용 안정성 기준에서는 공격적인 편이다.

판단:

```text
효과는 크지만 위험도도 있음.
현재 앱에 바로 반영하기보다는 보류 또는 실험 옵션으로 두는 편이 안전하다.
```

### 2. 실패 후보 우선순위 낮추기

SSH 연결에 실패한 IP를 완전히 제외하지 않고, 같은 MAC을 가진 다른 후보가 함께 보일 때만 뒤로 미룬다.

예:

```text
ARP 후보: 21, 174
방금 21 접속 실패
-> 다음 선택 순서: 174, 21
```

장점:

- 정답 IP를 완전히 버리지 않는다.
- 같은 MAC이 여러 IP에 남아 있는 stale 상황에서는 더 새 후보를 먼저 시도할 수 있다.
- 완전 제외보다 안전하다.

단점:

- 후보가 하나만 보이면 여전히 stale IP에 묶일 수 있다.
- 새 IP가 아직 네트워크에 안 보이면 개선 효과가 없다.

판단:

```text
현재까지 나온 개선안 중 가장 보수적이고 앱 반영 가능성이 높다.
다만 단독 stale 후보 문제는 해결하지 못한다.
```

### 3. 실패 후보 단독 반환 지연

방금 SSH 연결에 실패한 IP가 다음 탐색에서 혼자만 보이더라도, 즉시 확정하지 않고 nmap 전체 탐색 1 pass를 한 번 돌려 다른 후보가 새로 생겼는지 확인한다.

흐름:

```text
1. MAC 후보 A 발견
2. A로 SSH 재시도 실패
3. A를 suspect 후보로 기록
4. 다음 탐색에서 A만 보임
5. A를 바로 반환하지 않고 nmap 1 pass 확인
6. 다른 후보 B가 나오면 B 우선
7. 아무것도 안 나오면 A를 다시 허용
```

장점:

- 단독 stale IP에 계속 묶이는 문제를 완화할 수 있다.
- 정답 후보 A를 완전히 버리지는 않는다.
- 새 IP가 뒤늦게 드러나는 순간 잡을 기회를 만든다.

단점:

- A가 진짜 정답이고 SSH만 늦게 뜬 경우, nmap 1 pass 시간이 추가된다.
- 오늘처럼 nmap 1 pass가 40초대까지 걸리는 환경에서는 체감이 나빠질 수 있다.

판단:

```text
안전성과 개선 효과 사이의 절충안.
다만 배포 전 수동 테스트가 더 필요하다.
```

### 4. 전체 ARP 갱신용 ping sweep

전체 로컬 대역에 짧은 ping을 보내 ARP 캐시 갱신을 유도한 뒤 다시 탐색한다.

테스트 방식:

```text
192.168.111.1~254
ping -n 1 -w 200ms
동시성 32
```

테스트 결과:

```text
전체 sweep 비용: 약 3~4초
일부 케이스에서는 목표 IP가 ARP에 올라오지 않음
82 테스트에서는 nmap pass=4까지도 못 찾다가 다음 attempt에서야 발견
```

장점:

- 특정 IP를 알고 직접 찌를 때는 ARP 갱신 효과가 있었다.
- 수동 SSH가 새 IP를 깨우는 것과 비슷한 효과를 기대할 수 있다.

단점:

- 앱은 새 IP를 미리 모르기 때문에 전체 sweep을 해야 한다.
- 전체 sweep이 항상 새 IP를 드러내지는 못했다.
- 약한 AP에서는 불필요한 트래픽으로 볼 수 있다.

판단:

```text
항상 실행하기에는 애매하다.
stale 의심 상황에서만 보조 수단으로 실험할 수 있다.
```

### 5. 실패 유형별 분기

SSH 실패를 한 덩어리로 보지 않고, 실패 유형에 따라 다르게 처리한다.

예:

```text
SSH banner timeout:
  IP는 맞는데 sshd가 늦게 뜬 가능성이 있음
  같은 IP 재시도 유지

TCP timeout / no route / connection refused:
  stale IP 또는 네트워크 미반영 가능성이 큼
  실패 후보 기록 또는 우선순위 낮추기
```

장점:

- 정답 IP를 성급하게 버릴 위험을 줄인다.
- "찾았지만 sshd가 늦은 경우"와 "IP 자체가 잘못된 경우"를 조금 더 구분할 수 있다.

단점:

- Paramiko/Windows socket 예외가 환경마다 다르게 나올 수 있다.
- 너무 복잡하게 나누면 유지보수가 어려워진다.

판단:

```text
3번 개선과 함께 쓰면 가장 현실적이다.
단, 예외 메시지 기반 분기는 과하게 세분화하지 않는 편이 좋다.
```

## 현재 추천안

바로 앱에 넣는다면 다음 순서가 가장 안전하다.

```text
1. 새 IP 후보로 SSH 접속을 시도한다.
2. 기존처럼 새 IP에 대해 5회 재시도한다.
3. 최종 실패한 IP를 프로파일별 failed candidate로 기록한다.
4. 다음 탐색에서 같은 MAC 후보가 여러 개면 failed candidate를 뒤로 미룬다.
5. 후보가 failed candidate 하나뿐이면 바로 버리지는 않는다.
6. 단, 같은 failed candidate가 여러 차례 반복되면 nmap 1 pass로 다른 후보가 있는지 확인한다.
```

이 방식은 "정답 IP를 완전히 제외하지 않는다"는 안전성을 유지하면서, 이전 IP stale 반복으로 1~3분까지 늘어지는 케이스를 줄이는 절충안이다.

## 보류할 것

다음은 아직 앱에 바로 넣지 않는 편이 좋다.

```text
1. 실패 IP 20초 완전 제외
2. nmap 추가 병행 실행
3. 매번 전체 ping sweep
4. arp -d 자동 실행
```

특히 `arp -d`는 특정 stale IP에는 효과가 있을 수 있지만, Windows 권한과 현장 네트워크 영향이 있어 배포용 기본 동작으로는 신중해야 한다.

## 결론

v4의 핵심 탐색 방식은 유지한다.

```text
ARP 캐시 확인
nmap 6슬롯 탐색
mDNS 보조
MAC 일치 검증
새 IP SSH 재시도
```

추가 개선은 "더 공격적으로 찾기"보다 "방금 실패한 후보를 얼마나 믿을지 조절하기"에 초점을 둔다.

현재 가장 안전한 후보는:

```text
실패 후보 우선순위 낮추기 + 반복 실패 시 nmap 1 pass 확인
```

이다. 다만 오늘 테스트처럼 새 IP 자체가 공유기/AP에 늦게 드러나는 경우는 앱만으로 즉시 해결할 수 없고, 찾을 때까지 반복하는 것만 가능하다.
