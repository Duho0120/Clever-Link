# 사내 SFTP/SSH 통합 접속 도구 — 개발 체크리스트

> **한 줄 정의**: RaiDrive의 사용 경험(프로파일 1회 등록 → 클릭 한 번으로 SFTP 드라이브 마운트)을 자체 구현하고, SSH 터미널 접속 기능을 추가한 사내 전용 Windows 앱
>
> **해결하는 문제**: SSH 접속 시 매번 CMD를 열어 주소·인증 정보를 재입력해야 하는 번거로움 제거. SFTP와 SSH를 하나의 프로그램에서 프로파일(→ "원격지") 하나로 관리

---

## 확정된 사항 (변경 시 이 문서 갱신)

| 항목 | 확정 내용 | 확정일 근거 |
|---|---|---|
| 대상 플랫폼 | Windows GUI 전용 | Jetson은 접속 "대상"(시나리오 A), Linux 클라이언트 불필요 |
| SFTP 마운트 | rclone (MIT) + WinFsp | 사내 전용이라 GPL 배포 의무 없음 |
| SSH 터미널 | paramiko + xterm.js | CMD 대체 |
| 인증 방식 | 비밀번호·개인키 **둘 다** 지원 | 사내 혼용 확인됨 |
| 비밀 보관 | keyring → Windows 자격 증명 관리자 | 평문 저장 금지 |
| 테스트 환경 | WSL Ubuntu (설치 가능 확인 ✓) | 사내 서버는 최종 확인용으로만 |
| WinFsp 설치 | 가능 확인 ✓ | — |
| 배포 | PyInstaller 단일 실행파일 + rclone 동봉 + WinFsp 자동 설치 | 버전 고정으로 강제 업데이트 없음 |

## 기술 구성 요약

| 역할 | 부품 | 우리가 작성? |
|---|---|---|
| SFTP → 드라이브 변환 | rclone | ❌ 동봉만 |
| Windows 드라이브 문자 생성 | WinFsp | ❌ 자동 설치 |
| SSH 터미널 연결 | paramiko | 호출 코드만 |
| 인증 정보 보관 | keyring | 호출 코드만 |
| 앱 창 | pywebview | ✅ 화면 작성 (메인 창 + 터미널 전용 창) |
| 터미널 화면 | xterm.js | 연결 코드만 |
| 화면 ↔ Python 통로 | websockets (마운트/터미널 공통 xterm 릴레이), pywebview js_api (프로파일·마운트 제어) | ✅ 서버 코드 |
| 테스트 서버 | WSL Ubuntu (개발용) + TEST(사내 실제 GPU서버, 192.168.120.10) | 설정만 |
| 배포 | PyInstaller | ✅ 빌드 스크립트 |

---

# Phase 0 — 준비 (완료)

## ☑ 0-1. WSL 테스트 서버 구축 — 완료
## ☑ 0-2. rclone 맨손 마운트 — 완료
## ☑ 0-3. paramiko 접속 테스트 — 완료

## ✅ 관문 1 — 부품 전부 단독 동작 (통과)

---

# Phase 1 — 코어 로직, 화면 없음 (완료)

## ☑ 1-1. 프로파일 저장소 — 완료 (profile_store.py)
## ☑ 1-2. 마운트 제어 모듈 — 완료 (mount_control.py)
## ☑ 1-3. SSH 세션 모듈 — 완료 (ssh_session.py)

## ✅ 관문 2 — 스크립트만으로 전 기능 동작 (통과)

---

# Phase 2 — 화면 (완료)

## ☑ 2-1. 프로파일 목록 UI — 완료 (app_ui.py)
## ☑ 2-2. WebSocket 서버 — 완료 (terminal_server.py)
## ☑ 2-3. xterm.js 터미널 화면 — 완료 (index.html → 이후 F-5에서 terminal.html로 분리)

## ✅ 관문 3 — 클릭만으로 마운트·터미널 (통과)

---

# Phase 3 — 마무리

## ☑ 3-1. 종료 처리 — 완료
## ☑ 3-2. 패키징 — 완료 (테스트/개발 단계용)
- [ ] 콘솔 창 숨기기(`--windowed`) — 정책상 3-3 검증 완료 후 마지막에 적용 예정, 아직 보류
- [ ] `--onefile` 시작 속도 개선(storage_path 고정) 재검증 — 미확인 상태로 남아있음

## 🔶 3-3. 실전 검증 (사내 서버 최초 접속) — 부분 완료
- [x] 사내 서버(TEST, jaeju@gpusvr0830, 192.168.120.10) SSH 접속 성공 (`whoami` → `jaeju` 확인)
- [x] 사내 서버 SFTP 마운트 성공 (비밀번호 방식, 홈 디렉터리 실제 파일 목록 확인)
- [x] 사내 서버 실제 볼륨 목록 조회 성공 (`df -hP`) — `/`(1.8T), `/data`(29T), `/NAS`(104T) 확인
- [ ] 키파일 방식으로 TEST 접속 테스트 (지금까지는 비밀번호 방식만 검증)
- [ ] 대용량 파일(수 GB) 읽기 속도 체감 확인
- [ ] Jetson 프로파일 등록 → SSH 터미널 접속 성공

---

# 🔴 직원 피드백 반영 사항 — **전부 완료** ✅

시연 후 받은 피드백. F-1~F-7 전부 Claude Code 세션에서 완료됨.

## ☑ F-1. 용어 변경 — "프로파일" → "원격지" — 완료
- `index.html`의 화면 노출 텍스트 5곳(제목, 추가 폼 제목, placeholder, 빈 목록 안내, 삭제 확인창)만 변경
- 코드 내부(`profile_store.py`, 변수명 `profile` 등)는 그대로 유지 (요청대로 화면 문구만 대상)

## ☑ F-2. 원격지 추가 시 자동 마운트 — 완료 (검증 중 버그 2건 발견·수정, 기능도 확장됨)
- [x] 원격지 추가 즉시 자동 마운트
- [x] 드라이브 문자 자동 배정
- [x] **검증 중 핵심 버그 발견 및 수정**: `find_current_drive_for_profile()`가 rclone `mount/listmounts`의 `Fs` 필드에서 host=/user= 문자열을 찾으려 했으나, rclone이 즉석 원격을 `:sftp{해시}:` 형태 내부 캐시 키로만 보여줘서 절대 매칭 안 되는 구조적 버그였음 → 항상 `None` 반환 → "기존 마운트 먼저 해제 후 전환" 로직이 한 번도 실행 안 되고 같은 서버가 여러 드라이브에 중복 마운트되는 문제로 이어짐. `mount_state.json`에 `{프로파일명: {drive, remote_path}}`를 직접 관리하고 `is_mounted()`로 매번 재검증하는 방식으로 교체해 해결
- [x] **키파일 인증 버그 발견 및 수정**: Windows 절대경로(`C:\Users\...`)의 콜론(`C:`)을 rclone이 connection string 구분자로 오인해 `key_file` 값을 `C` 한 글자로 잘라버리는 문제 발견 → 값을 작은따옴표로 감싸서(`key_file='C:\...'`) 해결
- [x] **기능 확장(사용자 요청)**: 서버에 실제 존재하는 저장소 볼륨을 `df -hP`로 조회해서 드롭다운에 보여주는 기능 추가 (`ssh_session.get_remote_volumes()`) — 참고로 **실제 RaiDrive에는 이 기능이 없음**(RaiDrive는 경로를 텍스트로 직접 입력받음), 저희가 자체적으로 더 편의 기능을 추가한 것
- [x] "다른 사람이 이미 쓰고 있는 볼륨 제외" 기능은 **범위 밖으로 보류** (나중 실행 계획)

## ☑ F-3. 원격지별 버튼 구성 변경 — 완료
- [x] 버튼을 **[폴더] [터미널] [삭제]** 3개로 축소, [마운트]/[해제] 버튼 제거
- [x] 볼륨 드롭다운에서 항목 선택 시 `onchange`로 즉시 자동 (재)마운트 (버튼 없이 처리)
- [x] [폴더] 버튼: 아직 마운트 안 됐으면 현재 선택된 볼륨으로 자동 마운트 후 탐색기 오픈
- [x] 같은 프로파일 볼륨 전환 시 **같은 드라이브 문자를 재사용**하도록 처리 (매번 새 문자 배정 안 함)
- [x] 상태 메시지에서 Windows 드라이브 문자(`Z:` 등) 노출 제거, 볼륨 경로만 표시 (`'TEST' -> /data 마운트 완료`) — 정확한 볼륨 라벨을 위해 `mount_state.json`에 `remote_path`도 함께 기록하도록 확장

## ☑ F-4. "추가" 버튼을 우측 상단으로, 별도 입력창으로 분리 — 완료
- [x] 화면 우측 상단에 **[+ 추가]** 버튼 신설, 상시 노출 폼 제거
- [x] 클릭 시 입력 필드를 담은 **모달**(배경 클릭 또는 [취소]로 닫힘) 표시
- [x] 브라우저로 직접 렌더링해 모달 열기/닫기 동작 확인

## ☑ F-5. 터미널 연결 창을 별도 윈도우로 분리 — 완료 (1차 시도 실패 → 원인 파악 후 재수정 완료)
- [x] 터미널 전용 페이지(`terminal.html`) 신설, `webview.create_window()`로 완전히 새 창에서 띄움
- [x] **1차 구현 실패**: 프로파일 이름을 `file:///...html?profile=이름` 쿼리스트링으로 전달했더니 WebView2가 `ERR_FILE_NOT_FOUND`로 파일을 못 찾음
- [x] **원인**: 메인 창(`index.html`)은 순수 파일 경로만 `url`로 넘기는 방식인데, 터미널 창만 `file://` 접두사 + 쿼리스트링을 직접 조합해서 넘긴 게 WebView2와 호환 안 됨
- [x] **수정**: 창마다 별도의 `js_api` 인스턴스(`TerminalApi`)를 만들어 프로파일 이름을 들고 있게 하고, `terminal.html`이 `pywebviewready` 이후 `get_profile_name()`으로 물어보는 방식으로 교체 (메인 창과 동일한 패턴으로 통일)
- [x] `js_api` 스텁 주입 + `pywebviewready` 이벤트 발생시켜 실제 WebSocket 연결·SSH 배너 수신까지 검증
- [x] 사용자가 실제 앱에서 터미널 새 창 분리 확인 완료

## ☑ F-6. 터미널 세션 섞임 버그 — 완료 (이전 세션에 이미 완료, 이번 세션에 F-5 재구조화 시 로직 유지)
## ☑ F-7. 앱 꺼도 마운트 유지 (트레이 아이콘) — 완료 (이전 세션에 이미 완료)

---

## 🔶 3-4. 배포 — 핵심 부분 완료 (임시 방식으로 실제 전달까지 진행함)
- [x] 콘솔 창 숨기기(`console=False`) 완료
- [x] PyInstaller `--onedir`로 전환 완료 (실행 준비 시간 ~10초 → ~2~4초)
- [x] WinFsp에 이어 **WebView2 Runtime 자동 확인·설치**(`webview2_check.py`)도 추가 — 둘 다 최초 1회만 확인하고 flag 파일로 다음 실행부터 건너뜀
- [x] 소스 → 테스트 폴더 → 배포 폴더(`SFTP_SSH_직원배포용`) 3단계 검증 절차로 실제 첫 전달 진행 (폴더 압축 + 안내 문구 전달 방식)
- [ ] Inno Setup으로 정식 설치 프로그램 제작 — 지금은 "폴더 압축해서 전달"이 임시방편이라, 정식 설치 프로그램(시작 메뉴/바탕화면 바로가기, 프로그램 추가/제거 등록)으로 교체하는 건 여전히 남아있음
- [ ] 사용 안내 1페이지 문서화 (지금은 대화로 정리한 안내 문구를 그때그때 복사해서 전달 중)

---

# Phase 4 — Ward/Room 계층 구조 (병원 다중 서버 관리, 신규 요구사항)

📝 **배경**: 이 프로그램이 병원 내부용으로 실제 배포될 예정임을 안내받음. 첨부된 표 기준으로 Ward(병동) 3개 아래에 총 131개의 Room(개별 병실/구역)이 있고, Room 하나하나가 실제 서버(Host name, 예: co1~co32)에 대응됨.

**확정된 개념 (2026-07-20)**:
- **Ward는 원격지가 아니라, 원격지를 고르기 위한 상위 필터**. Ward 자체에 접속하지 않음.
- **Room이 실제 원격지** (기존에 [+ 추가]로 하나씩 등록하던 서버 하나하나에 해당).
- Ward 드롭박스에서 아무것도 선택 안 한 초기 상태에는 **원격지 목록이 비어있어야 함** (131개를 한꺼번에 보여주지 않음 — 애초에 Ward로 나누려는 목적과 배치되고, 어떤 Ward를 기본으로 보여줄지 정할 기준도 없음).
- Ward 하나를 선택하면, 그 Ward에 속한 Room들만 원격지 목록에 나타남 (다른 Ward의 Room은 안 보임).

**사내 서버 접근은 지금 당장 요청하지 않기로 함** → 로컬 WSL 테스트 환경으로 대체 검증 진행.

## ☑ 4-1. 로컬 테스트 환경 구축 — 완료
- [x] 사용자로부터 샘플 Ward/Room/Host name 데이터 전달받음 (S서울 3개, 포항세명기독병원(62) 2개, 포항세명기독병원(66) 3개 — 총 8개 Room, 고유 Host 6개: A0/A1/B0/C0/D0/D1)
- [x] WSL에 Host name 개수만큼 로컬 계정 생성 (`useradd`) — IP/포트를 여러 개 안 만들고, **계정을 여러 개 만들어서** 각 Room이 서로 다른 실제 대상에 붙는지 구분 가능하게 함 (같은 서버, 다른 계정 = `whoami`로 구분 가능)
- [x] Ward/Room/Host 매핑 테스트 데이터 파일 작성 (`ward_test_mapping.json`, host는 전부 WSL 127.0.0.1, username만 계정별로 다르게)
- [x] 계정별 SSH 접속 확인 — A0~D1 전부 `whoami`가 각자 이름 그대로 나오는 것 확인
- 📌 **완성된 도구**: `setup_ward_test_env.py` — `WARD_ROOM_HOST_DATA`에 Ward/Room/Host 목록을 넣고 실행하면 WSL 계정 자동 생성 + 매핑 JSON 자동 생성. 이미 있는 계정은 건드리지 않아 재실행해도 안전.

📝 **트러블슈팅 기록 (중요, 재사용 시 참고)**:
- WSL에서 `sudo`가 비밀번호를 요구하는데, 이 스크립트는 TTY 없이 비대화형으로 실행돼서 그냥 `sudo`를 쓰면 응답 없이 멈춤 → `echo '비밀번호' | sudo -S ...`로 표준입력을 통해 비밀번호를 흘려보내는 방식으로 해결
- `chpasswd`가 이 WSL의 PAM 설정에서 "Authentication token manipulation error"로 매번 실패함 → `usermod -p`로 비밀번호 해시를 직접 설정하는 방식으로 우회
- **가장 크고 오래 걸린 문제**: `wsl.exe`에 `-c` 인자로 `$` 문자가 포함된 명령 문자열(비밀번호 해시 등)을 직접 넘기면, `$6`이나 `$abc` 같은 부분이 마치 정의 안 된 변수처럼 통째로 사라지는 문제가 있었음. Python `subprocess.run`으로 셸을 안 거치고 직접 호출해도 똑같이 발생해서, 원인은 wsl.exe 자체(또는 그 인자 전달 경로) 어딘가로 추정됨(정확한 원인은 못 찾음). **해결**: `$`가 들어가는 명령은 절대 `-c` 인자로 직접 넘기지 않고, 임시 `.sh` 파일에 내용을 써서 그 파일을 실행하는 방식(`bash '/mnt/c/...'`)으로 우회 — 파일 "내용"을 읽어서 해석하는 거라 인자 전달 경로를 안 거치기 때문. 앞으로 WSL에 `$`가 들어간 명령(비밀번호, 해시, 특수문자 포함 값 등)을 보낼 일이 있으면 이 패턴을 재사용할 것
- `/mnt/c/...` 같은 WSL 경로를 `wsl.exe`에 독립된 인자로 바로 넘기면(예: `bash "/mnt/c/..."`) Git Bash가 이걸 자기 자신의 경로로 오인해서 앞에 자기 설치 경로를 붙여버림(`C:/Program Files/Git/mnt/c/...`) → `-c "... 'path' ..."`처럼 하나의 문자열 안에 인용부호로 감싸 넣으면 해결됨

## ☑ 4-2. 데이터 모델 설계 — 완료
- [x] Ward/Room 매핑은 별도 파일 없이 **`profiles.json`에 `ward` 필드 추가**하는 방식으로 결정 (인증/keyring 로직 재사용 위해)
- [x] Room = 프로파일 이름 그대로, `ward` 필드만 추가
- [x] 기존 수동 등록([+ 추가])과 Ward 소속 원격지 공존 확인 (추가 모달에 Ward 입력 필드, 자동완성+새 이름 입력 가능)

## ☑ 4-3. UI 구현 — 완료
- [x] Ward 드롭박스 추가 (화면 상단, 선택 전엔 "Ward를 선택하세요" 안내만 표시)
- [x] Ward 선택 시 해당 Ward의 Room들만 원격지 목록에 필터링되어 표시 (`get_profiles_in_ward`)
- [x] 기존 원격지 목록 UI(볼륨 드롭다운, [폴더][터미널][삭제] 버튼)와 자연스럽게 연결

## ☑ 4-4. 검증 — 완료 (WSL 테스트 계정 기준)
- [x] Ward 전환 시 목록이 정확히 필터링됨 (다른 Ward의 Room 안 섞임)
- [x] 서로 다른 Room 선택 시 실제로 다른 서버(계정)에 연결됨 (`whoami`로 확인, A0~D1 6개 계정 전부 검증)
- [ ] 기존 F-1~F-7 기능이 Ward 하위 Room에도 동일하게 잘 동작하는지 — 테스트 폴더(Ward만 포함, 구글 시트 제외 버전)에서 사용자가 직접 재확인 예정

---

# Phase 4-4 확장. 구글 시트 자동 연동

📝 **배경**: 사내 구글 시트(기기명/Ward/Room/Host name/IP/ID/PW 컬럼)에 새 행을 입력하면, 앱이 자동으로 그 원격지를 등록해야 함. 시트는 "링크 있는 사람에게 공개" 상태.

## ☑ 기본 골격 — 완료 (`sheet_sync.py`)
- [x] `SHEET_CSV_URL`(공개 CSV 링크)을 주기적으로(60초) 폴링 + 수동 "↻ 시트 새로고침" 버튼
- [x] 새 행 → 원격지 추가, 기존 행 수정(IP/PW/Ward 등) → 자동 갱신
- [x] 시트에서 행이 사라져도 자동 삭제는 안 함 (안전장치)
- ⚠️ **시트 링크는 `edit?usp=sharing`가 아니라 `/export?format=csv`로 바꿔야 실제로 CSV가 내려옴** (처음에 이 착오로 한참 헤맸음)
- ⚠️ **CSV 맨 윗줄이 "클레버러스 엣지 디바이스 목록" 제목 병합 줄**이라, 무조건 첫 줄을 헤더로 삼으면 깨짐 → "Ward"+"Room"이 실제로 있는 행을 헤더로 탐지하도록 처리함
- ⚠️ **`text.splitlines()`로 줄 단위 처리하면 안 됨** — Ward 병합 셀처럼 따옴표로 감싸인 셀 안에 실제 줄바꿈이 있으면 그 셀이 두 줄로 쪼개져서 CSV 구조가 깨짐. 반드시 `csv.reader`로 먼저 전체를 파싱해서 "행" 단위로 다뤄야 함

## ☑ 실제 시트 데이터 확인 중 발견한 예외 처리 — 전부 완료 (2026-07-21)

1. **[x] 포트 자동 분리**: IP 칸이 `loclx.io:12233`처럼 `주소:포트` 형태면 콜론 기준으로 나눠서 실제 포트 사용, 없으면 22 기본값 (`sheet_sync._parse_host_port`)
2. **[x] Room 병합 셀 자동 분할**: `"401\n402"`처럼 줄바꿈으로 여러 개 들어있으면 같은 접속 정보를 공유하는 별도 원격지 여러 개로 자동 분할 (`sheet_sync._split_rooms`)
3. **[x] 로컬 IP + 터널 주소 동시 지원**: 원격지 하나로 유지하고, `profile_store.py`에 `host_alt`/`port_alt` 필드 추가. 새 모듈 `host_resolver.py`가 실제 접속 시점(SSH/마운트 둘 다)에 기본 주소를 1.5초 짧게 테스트 → 안 되면 보조 주소로 자동 전환. `ssh_session.connect_profile`, `mount_control._build_remote_string` 둘 다 연결
4. **[x] 비밀번호 빈칸 기본값**: PW 칸이 비어있으면 `DEFAULT_PASSWORD = "959811!!"` 사용
5. **[x] 연결 불가 원격지 경고 표시**: `sheet_sync._parse_rows`가 동기화할 때 Ward별로 집계, `Api.get_sheet_issue_count(ward)`로 조회, `index.html`이 Ward 선택 시 빨간 글씨로 개수 표시 ("시트를 확인해주세요!")

📝 **구현 중 실제 시트 데이터로 검증하다가 추가로 발견해 고친 버그 2건 (예상 못 했던 것)**:
- **Room 병합 셀 오판 버그**: `"silverking_48\n(lobby, hall)"`처럼 둘째 줄이 괄호로 감싸인 "설명 메모"인 경우까지 무조건 분할해버려서, `"(lobby, hall)"`이라는 이상한 이름의 가짜 원격지가 실제로 생성됐었음 → 괄호로 통째로 감싸인 줄은 분할 대상에서 제외하도록 수정
- **Room 번호가 서로 다른 Ward에서 우연히 겹치는 문제**: 실제 시트에 `"306"`이 "우리행복요양병원(3)"과 "경주굿모닝병원(3)" 두 곳에 각각 있었음. `profiles.json`은 이름 하나짜리 평평한 사전이라 나중 행이 먼저 행을 조용히 덮어써서 데이터가 유실됐었음 → 다른 Ward 소속으로 이미 등록된 이름과 충돌하면 `"306(경주굿모닝병원(3))"`처럼 Ward를 붙여 구분하도록 수정

✅ **실제 시트로 검증 완료**: 66개 원격지, 16개 Ward로 정상 동기화 확인 (`profiles.json`에 실제 반영). `host_resolver`의 보조 주소 자동 전환은 SSH 연결까지 실제로 성공하는 것 확인 (`whoami`로 검증).

📝 **이후 세션에서 추가로 반영된 것들(2026-07-22, 이 문서 미반영 상태였음 — 요약만)**: Room도 Ward처럼 병합 셀 이어받기 지원(단, Ward 경계에서 초기화), 같은 행 안 여러 Room("401\n402")은 분할이 아니라 쉼표로 합쳐서 원격지 1개로("401,402-co33"), 유령 프로파일 감지(시트에서 사라진 원격지를 자동 삭제 대신 화면에 경고+삭제버튼으로 표시), `save_profile()` 내용 동일하면 재저장 생략, **자동 폴링(시작 시 1회+60초 주기) 완전히 제거하고 수동 "새로고침" 버튼으로만 동기화하도록 변경**. 61개 원격지로 재검증됨.

## 🔲 계획 (미착수): Ward 드롭박스 — 개인 추가 Ward 최상단 고정

📝 **배경**: 원격지 추가/삭제는 이제 구글 시트로 관리하기로 확정. 다만 앱은 RaiDrive 대체 목적도 있어서 [+ 추가] 기능 자체는 유지 — 직원이 시트에 없는 자신의 개인 업무용 서버를 연결할 때 씀. 이때 기존 Ward를 고르는 대신 **새 Ward 이름을 즉석에서 만들 수 있어야** 함(입력칸이 이미 자유 입력이라 이 부분은 됨, 확인만 필요).

- [ ] Ward 드롭박스 정렬: 병원(시트 유래) Ward들의 기존 정렬 방식(가나다순)은 그대로 유지
- [ ] 대신 **개인이 만든 Ward만 따로 분리해서 드롭박스 맨 위에 고정 표시**
- 검토했던 대안(**보류**): 개인 원격지를 아예 Ward 목록 밖으로 빼서 화면 하단에 항상 보이는 별도 섹션으로 두는 방식 — 구조 변경이 커서 일단 보류, 위 방식(Ward 드롭박스 최상단 고정)으로 먼저 진행하기로 함

## 🔶 제안 예정 계획: "연동시트 변경" 기능 — 보안 + 유연성 둘 다 해결 (2026-07-22, 아직 미착수)

📝 **이 계획을 관철하는 두 가지 핵심 이유** (건의할 때 이 두 가지를 강조):

### 1. 🔒 보안 — 링크 한 줄로 회사 전체 서버 비밀번호가 새는 구조를 없앤다
지금은 시트가 "링크 있으면 누구나 열람 가능"으로 공개되어 있고, 앱은 이 공개 CSV 링크(`/export?format=csv`)로 데이터를 가져온다. 시트 안에는 전 기기의 ID/비밀번호(대부분 `959811!!`)가 평문으로 들어있어서, **이 링크 하나만 새어나가도 회사 전체 서버 접속 정보가 통째로 노출**된다. 링크는 "특정인에게만 준다"는 개념이 없어 유출 경로 추적도, 특정인만 접근 차단하는 것도 불가능 — 그래서 시트 링크 하나만 첨부하는 지금 구조는 위험하고, **연결 인증용 첨부 파일(서비스 계정 키)**을 별도로 요구하도록 바꿔야 한다.

### 2. 🔧 유연성 — 앱이 "이 링크 전용"으로 굳어있지 않게 한다
지금은 `SHEET_CSV_URL`이 소스 코드에 하드코딩되어 있어서, 다른 시트로 바꾸려면 코드를 고치고 다시 빌드해야 한다. **"연동시트 변경" 버튼**으로 링크를 바로 교체할 수 있게 만들면: (a) 나중에 우리 시트가 바뀌어도 코드 수정 없이 대응 가능 (b) 이 앱을 다른 팀/회사가 재사용할 때도 각자 자기 시트만 연결하면 바로 쓸 수 있음.

**구체적인 UI/동작 설계**:
- [ ] 화면에 **"연동시트 변경"** 버튼 추가
- [ ] 클릭 시 모달: **시트 URL 입력칸** + **연결 키 파일 첨부 버튼(선택 사항, `.json`)**
  - 시트 URL만 있고 키가 없으면 → 공개 링크 방식으로 바로 연동 (지금처럼)
  - 키까지 첨부되면 → 서비스 계정 인증 방식으로 연동 (비공개 시트도 가능)
- [ ] **입력받은 URL을 그대로 믿지 않고, URL 안의 시트 ID만 정규식으로 뽑아서 항상 우리가 `/export?format=csv` 형태로 재조립** — 사용자가 `edit?usp=sharing` 같은 일반 공유 링크를 붙여넣어도 실패하지 않도록 (저희도 처음에 이 실수로 헤맸던 부분, 사용자 입력에서는 반드시 방지해야 함)
- [ ] 첨부된 키 파일은 안전한 로컬 위치로 복사해서 경로만 저장 (원본 파일 위치가 나중에 사라져도 안전하게)
- [ ] `sheet_sync.py`의 `fetch_sheet_rows()`만 이 설정을 읽어 분기 처리(키 유무에 따라 공개 CSV 방식 vs. 서비스 계정 API 방식) — 그 아래 `_parse_rows`/`sync_from_sheet` 로직은 무관, 안 건드림
- [ ] 시트를 통째로 교체해도 옛 시트의 원격지들은 이미 있는 **유령 프로파일 감지 기능**이 자동으로 잡아주므로(자동 삭제는 안 하고 화면에 경고+삭제 버튼), 이 부분은 추가 개발 없이 그대로 재사용됨

**추가 확정 (2026-07-23): "단일 활성 시트"가 아니라 "등록된 시트 목록에서 드롭박스로 자유롭게 선택"하는 방식으로 확장**
- [ ] 시트를 바꿀 때마다 예전 URL/키를 덮어써서 잃어버리는 게 아니라, **입력했던 URL+키 조합을 목록에 계속 누적 저장**
- [ ] "연동시트 변경" 모달에 **등록된 시트 목록을 드롭박스로 표시**, 그중 하나를 골라 바로 전환 가능 (완전히 새 시트를 추가하는 것도 계속 가능)
- [ ] 저장 형식 변경: `sheet_config.json`이 단일 `{url, key_path}`가 아니라 **목록 `[{name, url, key_path}, ...] + 현재 활성 항목`** 구조로
- [ ] 시트를 전환할 때마다 이전 활성 시트의 원격지들은 (위와 동일하게) 유령 프로파일 감지로 자동 정리 대상이 됨 — 목록 방식으로 바뀌어도 이 부분은 그대로 재사용

⚠️ **중요: 서비스 계정 키 ≠ 직원 개인의 시트 편집 권한** (헷갈리기 쉬운 부분, 반드시 구분해서 이해)
- 서비스 계정 키 파일은 **앱이 API로 시트를 "읽기"만** 하게 해주는 것 — 이 파일을 가진 직원이 브라우저로 `sheets.google.com`에 들어가 시트를 직접 열람/수정할 수 있게 되는 게 **아님**
- 시트를 직접 편집해야 하는 담당자는, **그 사람 개인 구글 계정을 시트 "공유" 설정에 별도로 편집자로 추가**해야 함 — 서비스 계정 설정과 완전히 독립적인 절차
- 최종 그림: (1) 서비스 계정은 뷰어 권한으로 앱 전체가 공통으로 씀 (2) 시트를 직접 고치는 특정 담당자만 개인 계정으로 편집자 초대 (3) 나머지 직원은 앱을 통해서만 자동으로 원격지 목록을 받고, 시트를 직접 열어볼 필요/권한 없음

📝 **동시 사용 관련(직원이 여러 명 동시에 앱을 써도 되는지)**: 서비스 계정은 로그인 세션 개념이 없고 매 API 호출마다 키로 서명하는 방식이라, **여러 직원이 각자 앱을 동시에 켜고 새로고침해도 서로 간섭 없이 독립적으로 동작**함 (지금의 공개 링크 방식과 사용 경험은 동일, 접근 범위만 좁아짐). 사람마다 개별 로그인/승인받는 절차는 필요 없음.

📝 **참고 (RaiDrive 비교, 2026-07-22 확인)**: RaiDrive의 가져오기/내보내기 기능은 PC 간 설정 이전을 위한 1회성 암호화 `.dat` 파일 방식일 뿐, 외부 URL/시트와 실시간 동기화하는 기능은 없음 — "연동시트 변경"은 RaiDrive에도 없는 이 앱만의 차별점이 됨.

---

# 심화 검증 리스트 — 전부 미착수 (3-3과 함께 진행 권장)

## 대용량 파일 처리
- [ ] 수 GB급 파일 열기/저장 속도 체감
- [ ] `--vfs-cache-mode full`의 로컬 디스크 사용량 체크
- [ ] 로컬 캐시 저장 위치 명시적 지정 여부 확인
- [ ] 다운로드 진행률 표시 없음 — 실사용성 확인 필요
- [ ] 서버 A → 서버 B 복사 시 "다운로드 후 업로드"로 두 배 느려질 가능성
- [ ] 터미널 대량 텍스트 출력 시 xterm.js/WebSocket 버벅임 여부

## 파일시스템 특성 차이 (Linux ↔ Windows)
- [ ] 대소문자 구분 차이
- [ ] 경로 길이 260자 제한
- [ ] 심볼릭 링크 표시 방식
- [ ] 파일 소유자/권한의 Windows ACL 변환

## 네트워크 안정성
- [ ] 마운트 상태에서 절전/재연결 후 동작 (터미널 재접속 버튼은 확인됨, 마운트 쪽은 미확인)
- [ ] 동시에 여러 프로파일 마운트 시 안정성
- [ ] 같은 프로파일로 터미널 여러 개 동시 오픈 시 간섭 여부 (F-5로 별도 창이 됐으니 재확인 권장)

## 운영/유지보수
- [ ] 비정상 종료 후 유령 프로세스 정리 실제 테스트
- [ ] 로그 파일 저장 방안 (콘솔 숨기면 추적 방법 없어짐)

## 보안
- [ ] keyring 저장 비밀번호의 같은 Windows 계정 내 접근 가능성 재검토

---

# 부가 기능 검토 (RaiDrive 대비 필수/선택)

| 기능 | 필수/선택 | 상태 |
|---|---|---|
| 드라이브 문자 선택 | ✅ 필수 | 완료 (자동 배정으로 전환, F-3) |
| 인증 정보 저장(1회 등록) | ✅ 필수 | 완료 |
| 프로그램 시작시 자동연결 | 🔶 선택 | 미착수 |
| 읽기전용 모드 | 🔶 선택 | 미착수 |
| 파일잠금 | 🔶 선택 | 미착수 (F-2에서 "동시접속 제외"로 논의됐으나 보류) |
| 연결할 때마다 로그인 요구 | ❌ 불필요 | — |
| 로컬 디스크 공유 | ❌ 불필요 | — |
| 프록시 설정 | ⚠️ 확인 필요 | 미착수 |
| 클라우드 스토리지 종류 | ❌ 불필요 | — |
| 드라이브 문자 드롭다운 선택 | 🔶 선택 → 폐기 | F-3에서 아예 자동 배정으로 바뀌며 드롭다운 자체가 필요 없어짐 |
| 원격 폴더 선택(트리 탐색) | 🔶 선택 → **완료(간소화된 형태)** | 트리 탐색은 아니지만, F-2에서 `df -hP` 기반 **실제 서버 볼륨 목록 드롭다운**으로 구현됨. RaiDrive에도 없는 기능 |

---

# 운영 원칙

1. **관문을 건너뛰지 않는다.**
2. **개발은 전부 WSL 상대로.** 사내 서버(TEST)는 이번 세션에서 SSH·SFTP·볼륨 조회까지 검증했지만, 여전히 최소한으로만 접촉.
3. **막히면 에러 메시지 전문을 가지고 온다.**

# 다음 세션 진행 제안 순서

F-1~F-7 전부 완료됐으므로, 다음은 원래 순서대로:
1. **3-3 나머지** — 키파일로 TEST 접속, 대용량 파일 속도, Jetson 프로파일
2. **심화 검증 리스트** — 특히 대용량 파일/네트워크 안정성 항목 우선
3. **3-4 배포** — `--windowed` 전환, `--onedir`, Inno Setup

폴더 위치: `C:\Users\ASUS\Desktop\Sftp_Ssh`
관련 파일: `app_ui.py`, `mount_control.py`, `ssh_session.py`, `terminal_server.py`, `index.html`, `terminal.html`(F-5 신설), `profile_store.py`, `resource_path.py`, `winfsp_check.py`, `tray_icon.py`, `mount_state.json`(런타임 상태 파일, F-2 수정으로 신설)

---

# 핵심 내용 (취업/포트폴리오용 요약)

> ⚠️ 용어 정정: **"AI 활용"과 "업무 자동화"는 서로 다른 층위**입니다. 이 앱 자체에 AI/머신러닝 기능이 들어간 건 아닙니다 — 정확히는 **AI(Claude Code)를 활용해 개발 생산성을 높여서, 반복 업무를 없애는 자동화 소프트웨어를 직접 설계·개발**한 것입니다. 이 구분을 명확히 하고 서술해야 오해가 없습니다.

## Before — 무엇이 문제였는가

- 사내에서는 서버 접속 시 RaiDrive로 SFTP 드라이브를 마운트해서 사용해왔음. 병원별로 직접 방문하거나, 터널 주소를 통해 원격으로 접속.
- **SSH 터미널 접속은 RaiDrive로 안 되기 때문에, 접속할 때마다 IP·포트·계정·비밀번호를 매번 손으로 입력**해야 했음.
- 특히 **Jetson 기기가 다수 설치된 병원으로 출장** 갈 때, 기기 하나하나마다 접속 정보를 반복 입력해야 해서 **뚜렷한 병목이자 반복 피로**로 작용.
- 구글 시트에 병원별 서버 목록(IP, 터널 주소, Host name 등)을 이미 기록해두고 있었지만, **그 정보를 매번 시트에서 확인하고 옮겨 적어 접속**하는 수작업이 그대로 남아있었음 — 즉 데이터는 있었지만 "연결"이 안 되어 있었던 상태.

## After — 무엇을 만들었는가

- **SFTP + SSH를 하나로 통합한 사내 전용 Windows 앱**을 처음부터 설계·개발 (rclone/WinFsp 기반 드라이브 마운트 + paramiko/xterm.js 기반 터미널).
- **구글 시트 ↔ 앱 실시간 연동**: 시트에 서버 정보를 입력하면 별도 조작 없이 앱에 원격지로 바로 등록됨. 사람이 시트를 보고 옮겨 적는 과정 자체를 제거.
- **터미널 인증 자동화**: [터미널] 버튼 한 번이면 인증 정보 입력 없이 바로 터미널 창이 뜸 — 매번 손으로 입력하던 절차를 완전히 제거.
- **실제 데이터 기반 예외처리**: 병합 셀, 서로 다른 병동의 Room 번호 충돌, 로컬 IP·터널 주소 동시 지원, 잘못된 시트 입력 등 실제 회사 데이터를 붙여보며 발견한 문제들을 하나씩 재현·검증하며 해결.
- **삭제 방지/안전장치**: 시트에서 값이 사라져도 자동 삭제하지 않고 경고 후 사람이 확인하게 하는 등, 실수로 인한 데이터 유실을 막는 안전장치를 설계에 반영.
- 결과적으로 실제 업무에 투입되어 **높은 만족도**로 사내 배포 완료.

## 어필 포인트로 정리하면

| 관점 | 내용 |
|---|---|
| **업무 자동화** | 매 접속마다 반복되던 수작업(시트 확인 → 정보 복사 → 수동 입력)을 실시간 데이터 연동 + 원클릭 인증으로 완전 제거 |
| **병목 단축** | 특히 다수 기기가 몰려있는 출장/현장 상황에서 반복 입력에 들던 시간을 구조적으로 없앰 |
| **AI 활용 역량** | Claude Code를 활용해 요구사항 정의→구현→실제 데이터 기반 디버깅→배포까지 전 과정을 빠르게 진행, 실제 회사 데이터의 예외 케이스를 능동적으로 찾아내 검증하는 과정까지 주도 |
| **문제 해결 태도** | "안 된다"에서 멈추지 않고, 실제 원인(DNS 미등록 도메인, 회수된 기기, 시트 데이터 오류 등)을 직접 계측·검증해서 근본 원인까지 추적 |
