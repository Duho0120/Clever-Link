# SFTP/SSH 통합 접속 도구 — 개발 인수인계 문서

> 목적/배경, 엣지케이스 발견·해결 과정, 배포 절차는 이 문서에서 다루지 않음.
> 필요하면 [사내배포_정리본.md](사내배포_정리본.md), [개선사항_체크리스트.md](개선사항_체크리스트.md),
> [동적IP_재탐색_개선안_v4.md](동적IP_재탐색_개선안_v4.md), [테스트스크립트_StaleIP_로직.md](테스트스크립트_StaleIP_로직.md) 참고.

---

## 1. 아키텍처 / 파일 맵

`app_ui.py`가 유일한 진입점이며 pywebview 창을 띄우고 `Api` 클래스를 `index.html`에 js_api로 연결한다.
아래는 그 아래로 실제 import되어 실행되는 파일들이다 (`test_ssh.py`/`verify_app.py`류 독립 스크립트는 정리 완료).

### 1-1. 진입점 / 화면

| 파일 | 역할 |
|---|---|
| `app_ui.py` | 앱 진입점. pywebview 창 생성, `Api`(메인 창)/`TerminalApi`(터미널 창) js_api, 트레이 아이콘·백그라운드 워처 기동 |
| `index.html` | 메인 창 화면 (원격지 목록, Ward 필터, 추가/수정 모달, 각종 경고 배너) |
| `terminal.html` | SSH 터미널 전용 창 (xterm.js) |
| `style-v4.css` | 배포에 실제 쓰이는 유일한 스타일시트 |
| `terminal_server.py` | 터미널 창 ↔ SSH 세션을 잇는 WebSocket 서버 |
| `tray_icon.py` | 트레이 아이콘 (창을 X로 닫아도 백그라운드 유지, 완전 종료는 여기서만) |

### 1-2. 코어 로직

| 파일 | 역할 | 주요 함수 |
|---|---|---|
| `profile_store.py` | 원격지(프로파일) 저장소, `profiles.json` + keyring | `save_profile`, `get_profile`, `update_profile_field`, `list_wards`, `list_profiles_in_ward`, `find_duplicate_host_groups`, `find_profile_by_mac` |
| `mount_control.py` | rclone rc API로 SFTP 드라이브 마운트 제어 | `mount_profile`, `find_available_drive_letter`, `find_current_drive_for_profile`, `unmount`, `cleanup_all` |
| `ssh_session.py` | paramiko SSH 세션, 접속/재접속/볼륨조회/터미널 채널 | `connect_profile`, `_connect_profile_inner`, `get_remote_volumes`, `open_terminal_channel`, `_handle_changed_host_key` |
| `sheet_sync.py` | 구글 시트 → `profiles.json` 동기화 | `fetch_sheet_rows`, `_parse_rows`, `sync_from_sheet`, `get_ghost_profiles` |
| `host_resolver.py` | 기본주소/터널주소 중 실제로 살아있는 쪽 선택(1.5초 판정) | `resolve_reachable_address`, `is_reachable` |
| `resource_path.py` | PyInstaller 번들/개발 환경 양쪽에서 리소스 경로 통일 | `resource_path`, `app_dir` |

### 1-3. 동적 IP 재탐색 (nmap 기반)

| 파일 | 역할 | 주요 함수 |
|---|---|---|
| `mac_discovery.py` | nmap ARP discovery로 MAC 주소 일치하는 새 IP 탐색 (2단계: 넓게→촘촘하게) | `find_ip_for_mac`, `_scan_once`, `_find_ip_with_nmap_full_subnet_single_ip`, `get_mac_for_ip` |
| `mdns_discovery.py` | MAC으로 못 찾을 때 mDNS(zeroconf)로 보조 탐색 | `resolve_mdns_hostname`, `resolve_mdns_hostname_all` |
| `nmap_check.py` | nmap 미설치 시 winget 자동 설치 확인 (최초 1회) | `ensure_nmap`, `is_nmap_installed` |
| `background_watcher.py` | 같은 네트워크 대역 원격지만 대상으로 주기적 상태 점검 | — |
| `activity_log.py` | 재탐색/재접속 진행 상황을 앱 하단 상태줄에 실시간 표시 | — |

### 1-4. 신원/충돌 경고 상태 (배너)

같은 패턴(메모리 상태 + `mark()`/`clear()`)을 공유하는 5개 모듈. 전부 `index.html`의 배너 UI와 1:1 대응.

| 파일 | 언제 뜨는가 |
|---|---|
| `ip_rediscovery_state.py` | 재탐색 진행 중(노란 배너) / 재연결 성공(초록 배너) |
| `mac_mismatch_state.py` | 접속한 장비의 실제 MAC이 저장된 기준 MAC과 다를 때 |
| `mac_resolved_state.py` | 위 불일치가 다음 접속에서 다시 일치로 전환됐을 때(1회성 확인 배너) |
| `mdns_ambiguity_state.py` | mDNS 조회 결과 후보가 2개 이상이라 자동 선택을 안 했을 때 |
| `sheet_diverged_state.py` | 로컬 재탐색으로 주소가 자동 교정됐지만, 다음 시트 새로고침 때 되돌아갈 수 있음을 안내 |

### 1-5. 설치 확인 / 기타

| 파일 | 역할 |
|---|---|
| `winfsp_check.py` / `webview2_check.py` / `nmap_check.py` | 필수 구성요소 미설치 시 winget 자동 설치 안내 (각각 최초 1회) |

---

## 2. 핵심 기능 상세

### 2-1. 원격지 등록 → 자동 마운트
- 추가/수정 시 `app_ui.Api`가 `profile_store.save_profile()` 호출 후 즉시 `mount_control.mount_profile()`로 자동 마운트
- 드라이브 문자는 `find_available_drive_letter()`가 자동 배정, 같은 프로파일 재마운트 시 `mount_state.json`에 기록된 문자를 재사용 (rclone이 원격을 `:sftp{해시}:` 캐시 키로만 노출해서 `Fs` 필드로 직접 매칭이 안 되기 때문에 별도 상태 파일로 관리)
- 볼륨 목록은 `ssh_session.get_remote_volumes()`가 `df -hP`를 실행해 드롭다운으로 제공

### 2-2. Ward/Room 계층 구조
- `profiles.json`에 `ward` 필드만 추가하는 방식으로 구현 (별도 파일 없음)
- Ward는 원격지가 아니라 화면 필터: `profile_store.list_wards()` / `list_profiles_in_ward(ward)`로 조회
- Ward 미선택 시 원격지 목록 비어있음 (131개 한 번에 노출 방지가 목적)

### 2-3. 구글 시트 자동 연동
- `sheet_sync.sync_from_sheet()`가 공개 CSV(`/export?format=csv`)를 받아 `_parse_rows()`로 파싱 후 `profile_store.save_profile()` 반복 호출
- 원격지 이름은 `Room-HostName` 조합(`_parse_rows` 내부 규칙) — Host name까지 합쳐 같은 Ward 내 이름 충돌 방지
- `_parse_host_port()`가 `주소:포트` 형식 자동 분리, `_split_rooms()`가 같은 행 여러 Room을 쉼표로 합쳐 원격지 1개로 등록(괄호로 감싼 메모는 분할 대상에서 제외)
- Room/Ward 병합 셀은 위 행 값을 이어받되 Ward 경계에서 초기화
- 시트에서 사라진 원격지는 `get_ghost_profiles()`로 감지만 하고 자동 삭제하지 않음(화면에 경고+삭제 버튼)
- `source="manual"`(앱에서 직접 추가) 프로파일은 동기화 대상에서 제외
- 로컬 IP/터널 주소 동시 지원은 `host_resolver.resolve_reachable_address()`가 실제 접속 시점에 처리 (`ssh_session.connect_profile`, `mount_control._build_remote_string` 양쪽에서 호출)

### 2-4. SSH 터미널
- `terminal.html`(별도 창) ↔ `terminal_server.py`(WebSocket) ↔ `ssh_session.py`(paramiko) 구조
- 세션별 독립 `js_api`(`TerminalApi`)로 프로파일 이름 전달 (메인 창과 동일 패턴)
- 호스트 키가 바뀌면 `_handle_changed_host_key()`가 이전/새 지문을 보여주고 사용자 승인 후에만 갱신 (중간자 공격 가능성 때문에 자동 덮어쓰기 안 함)
- 연결 끊김 감지 5초 주기, 끊기면 5→8→12→20→30초... 간격으로 최대 10회 자동 재시도 (`ssh_session.py` 재접속 로직 + `terminal.html`/`terminal_server.py` 취소 흐름)

### 2-5. 동적 IP 자동 재탐색 (nmap 기반) — v4 핵심 기능
- 흐름: 기존 IP 접속 실패 → `mac_discovery.find_ip_for_mac()`으로 저장된 MAC과 일치하는 IP를 nmap ARP discovery로 탐색(1차 동시 96개로 넓게, 놓치면 2차 동시 32개로 촘촘하게) → 못 찾으면 `mdns_discovery`로 보조 탐색 → 찾은 IP로 재접속
- SSH 경로(`ssh_session._rediscover_new_ip`)와 마운트 경로(`mount_control._capture_mac`/`mount_profile`) 양쪽에 독립 적용, 마운트 쪽은 `host_resolver.is_reachable()`로 먼저 TCP 도달 여부를 확인해 "IP 문제"와 "비밀번호 문제"를 구분
- ARP 특성상 **같은 로컬 서브넷에 있는 장비만** 대상 (라우터/터널 경유 원격지는 대상 외)
- 접속 성공 시 MAC/hostname을 자동 캡처해 다음 재탐색에 사용 (`ssh_session.get_remote_mac`이 SSH로 직접 물어보는 방식을 우선 시도, 실패하면 ARP 방식(`mac_discovery.get_mac_for_ip`)으로 대체 — 라우팅/터널 경유 원격지에서도 동작하게 하기 위함)
- 진행 상황은 `activity_log.py`로 앱 하단 상태줄에, 재탐색/재연결 여부는 `ip_rediscovery_state.py`로 배너에 노출 (배포판은 `console=False`라 콘솔로는 확인 불가하기 때문)

### 2-6. 신원/충돌 안전장치
- **MAC 불일치**: 재탐색으로 찾은 IP의 실제 MAC이 저장된 기준과 다르면 `mac_mismatch_state`가 경고, 저장값은 자동으로 안 덮어씀 — "수정" 모달로 사용자가 직접 확인해야 해소. 다른 원격지의 기준 MAC과 겹치면 `profile_store.find_profile_by_mac()`으로 실제 어느 장비인지 짚어줌
- **mDNS 후보 다수**: 후보가 2개 이상이면 자동 선택하지 않고 `mdns_ambiguity_state`로 경고
- **시트-로컬 불일치**: 재탐색으로 로컬에서만 주소가 교정되면 `sheet_diverged_state`가 안내, 다음 시트 새로고침 시 자동 해소
- **중복 IP**: `profile_store.find_duplicate_host_groups()`가 host+port+ward+username이 모두 같은 원격지 조합을 앱 상단 배너로 경고 (계정/비밀번호 공유가 많아 엉뚱한 장비에 조용히 접속할 위험 방지 목적)
